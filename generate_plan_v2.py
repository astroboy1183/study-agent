#!/usr/bin/env python3
"""Build plan_v2.json (62 weeks x 7 days = 434 units) for the study agent.

Each week: 5 weekday theory units (Mon-Fri) each = concept + a curated video +
a coding rep; a Saturday deep-build project (blocks A/B/C); a Sunday
consolidation with a mastery question. Weeks are authored in batches in WEEKS;
re-run any time to regenerate the file. Only swap over ../plan.json when done.
"""
import json
import os

# Each week: title, days[5]={t,concept,watch,code}, build={title,A,B,C},
# cons={title,text,mastery}
WEEKS = [
 # ============================ PHASE 1 — DATA ENGINEERING ====================
 {"title": "Python for Data Engineering I", "days": [
   {"t": "The data model & idioms for pipelines",
    "concept": "Everything in Python is an object with an identity, type and value; names are references, not boxes, which is why mutability bites (aliasing a list vs copying it). The data model is the dunder methods (__len__, __iter__, __getitem__, __repr__, __eq__, __hash__) that let YOUR objects behave like built-ins and plug into for, len(), slicing and in.",
    "watch": "James Powell — 'So you want to be a Python expert?' (PyData); skim Fluent Python ch.1.",
    "code": "Build an immutable Record (with __repr__, __eq__, __hash__) and a Dataset collection implementing __iter__, __len__, __getitem__ so loops, len() and slicing all work."},
   {"t": "Iterators, generators & streaming data",
    "concept": "A generator (yield) is the easy way to write an iterator, suspending state between values to give lazy, constant-memory streams — the core DE trick for a 10GB file that won't fit in RAM. Chain generators source -> filter -> transform -> sink and lean on itertools for batching and windowing.",
    "watch": "David Beazley — 'Generator Tricks for Systems Programmers'.",
    "code": "Write a generator pipeline that streams a huge CSV line-by-line, filters and transforms lazily, and aggregates a metric; confirm flat memory with memory_profiler."},
   {"t": "Typing, dataclasses & pydantic schemas",
    "concept": "Type hints unlock static checking; dataclasses give cheap internal records; pydantic adds runtime validation and coercion. At every pipeline boundary you want a schema that rejects bad data loudly instead of letting a null propagate three stages downstream.",
    "watch": "mCoding — Python type hints; ArjanCodes — Pydantic tutorial.",
    "code": "Define a pydantic model for an input record (types, ranges, enums, a custom validator); feed it clean and dirty data and handle ValidationError without killing the batch."},
   {"t": "Context managers & robust error handling",
    "concept": "The with protocol (__enter__/__exit__, or @contextmanager) guarantees cleanup even when the body raises. Pair it with custom exception types, catch-narrow-not-broad, and retry-with-backoff for flaky I/O so one bad row or network blip doesn't kill a run — this is what separates a script from a pipeline.",
    "watch": "Corey Schafer — Context Managers; ArjanCodes — error-handling patterns.",
    "code": "Write a context manager that cleans up a resource on error plus a retry decorator with exponential backoff; prove the failure path works by injecting exceptions in a test."},
   {"t": "Testing data code (pytest, fixtures)",
    "concept": "Data code is testable code: pytest fixtures for setup/teardown, parametrize for table-driven cases, mocking for external services, and hypothesis for property-based tests that generate edge cases (empty, unicode, huge, negative). Untested pipelines fail silently in production; tested ones fail in CI.",
    "watch": "Corey Schafer — pytest; the Hypothesis 'Getting Started' docs/talk.",
    "code": "Write pytest tests + fixtures for your Record/Dataset and the CSV generator, add a parametrized case table, and one hypothesis property test (e.g. round-trip parse/serialize)."},
  ], "build": {"title": "Typed, tested data-ingestion library",
    "A": "Core: a generator-based reader that streams from CSV/JSON/an API, yielding validated pydantic records; lazy, constant memory, with a clean typed API ingest(source) -> Iterator[Record].",
    "B": "Robustness: context-managed resources, a retry-with-backoff wrapper for the API source, structured logging, and a configurable errors policy (skip-and-log vs fail-fast).",
    "C": "Ship: a Typer CLI, a full pytest suite (+ one hypothesis test), pyproject.toml and a README, then push it to GitHub as your first portfolio repo of the plan."},
  "cons": {"title": "Pythonic data code",
    "text": "The data model gives clean abstractions, generators give constant-memory streams, typing/pydantic guard your boundaries, context managers + retries give reliability, and pytest keeps it honest. Re-read your ingestion library and refactor one rough edge with fresh eyes.",
    "mastery": "Cold: how does a generator pipeline keep memory flat on a 10GB file, and exactly where in an ingestion flow do schema validation, retries and tests belong — and why there?"}},

 {"title": "Python for Data Engineering II", "days": [
   {"t": "Concurrency: threads, processes, asyncio",
    "concept": "The GIL means threads help I/O-bound work, not CPU-bound; use multiprocessing for CPU, asyncio for thousands of concurrent I/O waits. The skill is matching the model to the bottleneck.",
    "watch": "David Beazley — 'Python Concurrency From the Ground Up'; mCoding — asyncio.",
    "code": "Fetch 100 URLs three ways (threads, processes, asyncio), time each, and explain which wins and why."},
   {"t": "Performance & vectorization",
    "concept": "Python loops are slow; push work into C via NumPy/pandas vectorization or batching, and always measure before optimizing.",
    "watch": "Jake VanderPlas — 'Losing your Loops: Fast Numerical Computing with NumPy' (PyCon).",
    "code": "Rewrite a nested-loop numeric transform with NumPy vectorization and measure the speedup."},
   {"t": "Profiling CPU & memory",
    "concept": "cProfile and py-spy find CPU hotspots; memory_profiler and tracemalloc find leaks. Optimize the top of the flat profile, not guesses.",
    "watch": "mCoding — 'Python profiling'; search 'py-spy sampling profiler' (Ben Frederickson).",
    "code": "Profile a slow script with cProfile + py-spy, find the hotspot, and cut its runtime in half."},
   {"t": "Packaging & CLIs (Typer)",
    "concept": "pyproject.toml, virtual envs and building distributable tools; Typer/Click turn scripts into clean CLIs with types, subcommands and help.",
    "watch": "ArjanCodes — 'Python packaging'; the Typer docs tutorial.",
    "code": "Turn a script into an installable package with a Typer CLI (subcommands, --help, typed options)."},
   {"t": "Structured logging & configuration",
    "concept": "Logging over print (levels, handlers, structured/JSON logs), plus config via env/files (pydantic-settings). Observability starts here.",
    "watch": "mCoding — 'Modern Python logging'.",
    "code": "Add structured JSON logging + env-based config to your Week-1 ingestion library."},
  ], "build": {"title": "Profile & optimize a slow pipeline 10x",
    "A": "Take a deliberately slow data script (nested loops, per-row I/O, no batching) and profile it with cProfile + py-spy to find the real bottlenecks.",
    "B": "Optimize: vectorize the hot numeric path, batch the I/O, add concurrency where I/O-bound; re-measure after each change.",
    "C": "Write it up: a before/after benchmark table and a short note on what mattered and why — a clean 'performance engineering' portfolio post."},
  "cons": {"title": "Fast, reliable Python",
    "text": "Concurrency model, vectorization, profiling, packaging and logging turn scripts into tools. Re-profile your optimized pipeline and confirm the win is real, not luck.",
    "mastery": "When do threads help and when do they hurt in Python, and what is your first move when a pipeline is 'too slow'?"}},

 {"title": "Databases I — SQL & Relational", "days": [
   {"t": "Relational model & normalization",
    "concept": "Relations, keys and functional dependencies; normalize (1NF -> 3NF) to kill update anomalies, then denormalize deliberately for read speed.",
    "watch": "CMU Intro to Database Systems (Andy Pavlo) — the relational model lecture.",
    "code": "Design a normalized schema for orders/customers/products in Postgres with keys and constraints."},
   {"t": "SQL deep: window functions & CTEs",
    "concept": "Window functions (rank, lag, running totals) and CTEs (incl. recursive) are the tools that separate junior from senior SQL.",
    "watch": "Mode SQL tutorial (window functions); search 'SQL window functions explained'.",
    "code": "Solve 5 analytical queries using windows + CTEs: running totals, top-N per group, sessionization."},
   {"t": "Query planning & indexes (EXPLAIN)",
    "concept": "How the planner picks scans and joins, how B-tree indexes work, reading EXPLAIN ANALYZE, and when an index helps vs hurts.",
    "watch": "Hussein Nasser — database indexing / EXPLAIN; 'Use The Index, Luke' (Markus Winand).",
    "code": "Take a slow query, read EXPLAIN ANALYZE, add the right index, and show the plan change + speedup."},
   {"t": "Transactions, ACID & isolation levels",
    "concept": "ACID and isolation levels (read committed -> serializable) and the anomalies each prevents: dirty, non-repeatable and phantom reads; locking vs MVCC.",
    "watch": "CMU Pavlo — concurrency control; Hussein Nasser — isolation levels.",
    "code": "Reproduce a lost-update / non-repeatable-read anomaly in Postgres, then fix it by raising the isolation level."},
   {"t": "Postgres internals: MVCC, WAL, vacuum",
    "concept": "How Postgres does MVCC (tuple versions, xmin/xmax), the WAL for durability and crash recovery, and why VACUUM and bloat matter in practice.",
    "watch": "Search 'Postgres MVCC explained' (Hussein Nasser) and 'Postgres WAL'.",
    "code": "Inspect tuple visibility (xmin/xmax) with pageinspect, watch table bloat grow, then run VACUUM."},
  ], "build": {"title": "Schema design + a 100x query optimization",
    "A": "Design and load a realistic schema (a few million rows) — orders, users, events — with proper keys and constraints.",
    "B": "Find the 5 slowest analytical queries, read their plans, and optimize with indexes, rewrites and (where apt) materialized views — target 100x on at least one.",
    "C": "Document the before/after plans and numbers as a 'SQL performance' portfolio writeup."},
  "cons": {"title": "How a database thinks",
    "text": "The relational model, SQL power features, the planner + indexes, transactions and MVCC together explain how a DB stays fast and correct. Re-run your optimizations and reread the plans.",
    "mastery": "Walk through what happens from EXPLAIN to result for a slow join, and how MVCC lets readers not block writers."}},

 {"title": "Databases II — Storage & Formats", "days": [
   {"t": "Storage engines: B-tree vs LSM-tree",
    "concept": "B-trees are read-optimized and update in place; LSM-trees are write-optimized (memtable + SSTables + compaction). This is why Postgres and Cassandra/RocksDB chose differently.",
    "watch": "DDIA ch.3 concepts; CMU Pavlo — storage / tree indexes; 'LSM tree vs B-tree'.",
    "code": "Sketch a toy LSM: an in-memory memtable that flushes sorted runs to disk, with a simple read-merge."},
   {"t": "Columnar stores; OLTP vs OLAP",
    "concept": "Row stores serve transactions; column stores serve analytics via compression, vectorized scans and predicate pushdown — which is why your warehouse is columnar.",
    "watch": "CMU Pavlo — column stores; search 'columnar databases explained'.",
    "code": "Compare speed + size of the same aggregation as row (Postgres) vs columnar (DuckDB/Parquet)."},
   {"t": "The NoSQL landscape",
    "concept": "KV, document, wide-column and graph stores — what each is good and bad at, and when NOT to reach for them.",
    "watch": "Hussein Nasser — 'NoSQL explained'; DDIA ch.2.",
    "code": "Model the same domain in a KV, a document and a relational store; note what each makes easy or hard."},
   {"t": "Caching (Redis) & serialization",
    "concept": "Redis data structures and cache patterns (cache-aside, TTLs, and the hard part — invalidation); serialization format tradeoffs.",
    "watch": "Search 'Redis crash course' (Web Dev Simplified / Hussein Nasser).",
    "code": "Add a cache-aside layer (Redis) in front of a slow query; measure hit-rate and latency."},
   {"t": "File formats: Parquet, Avro, ORC, Protobuf",
    "concept": "Parquet/ORC are columnar (encoding, compression, footers, predicate pushdown); Avro/Protobuf are row-oriented with schemas. These are what your lakehouse lives on.",
    "watch": "Search 'Apache Parquet file format explained' and 'Avro vs Parquet'.",
    "code": "Write a dataset to CSV vs Parquet; compare size and read a single column with pushdown — measure the win."},
  ], "build": {"title": "Mini LSM-tree storage engine",
    "A": "Core: a memtable (sorted in-memory) that flushes to immutable on-disk SSTables when full; put/get/delete.",
    "B": "Reads + compaction: the read path merges memtable + SSTables newest-first (tombstones for deletes); a compaction that merges SSTables and drops dead keys.",
    "C": "Benchmark it vs a dict and vs SQLite; write up write-amplification and read-path tradeoffs — a standout systems portfolio project."},
  "cons": {"title": "Storage & serialization",
    "text": "Storage engines, columnar vs row, the NoSQL menu, caching and file formats are the physical layer under every pipeline. Re-read your LSM and note where a real engine adds a bloom filter or a WAL.",
    "mastery": "Why is an LSM-tree write-optimized and a B-tree read-optimized, and why is Parquet the right default for analytics?"}},

 {"title": "Distributed Systems I", "days": [
   {"t": "Reliability, scalability, maintainability",
    "concept": "The three concerns of data-intensive systems; measure load and latency with percentiles (not averages), and design for failure as the normal case.",
    "watch": "Martin Kleppmann — DDIA; his 'Distributed Systems' lecture 1 (Cambridge, on YouTube).",
    "code": "Instrument a service to record p50/p95/p99 latency under load; explain why the average lied."},
   {"t": "Data models & query languages",
    "concept": "Relational vs document vs graph models and their query languages, the impedance mismatch, and when each fits.",
    "watch": "DDIA ch.2; Kleppmann — data models lecture.",
    "code": "Model a many-to-many domain relationally and as documents; write 'friends-of-friends' in each and compare pain."},
   {"t": "Replication: leaders, followers, lag",
    "concept": "Single-leader replication, sync vs async, replication lag, and read-your-writes — why a follower can serve stale reads.",
    "watch": "DDIA ch.5; Kleppmann — replication lectures.",
    "code": "Simulate leader -> follower with artificial lag; demonstrate a stale read and a read-your-writes fix."},
   {"t": "Partitioning & sharding",
    "concept": "Partitioning by key range vs hash, hot spots, rebalancing, and routing a request to the right shard.",
    "watch": "DDIA ch.6; Kleppmann — partitioning lecture.",
    "code": "Build a hash-partitioned KV over N nodes; show a hot-key problem and mitigate it."},
   {"t": "CAP & consistency models",
    "concept": "CAP (really PACELC), linearizability vs eventual consistency, and why 'consistency' means different things to a DBA and to CAP.",
    "watch": "Kleppmann — 'Please stop calling databases CP or AP'; DDIA ch.9 concepts.",
    "code": "Demonstrate eventual consistency: concurrent writes to replicas converging (or not) under different rules."},
  ], "build": {"title": "Sharded KV store with a replication-lag sim",
    "A": "Build a hash-sharded in-memory KV across N simulated nodes with a client-side router.",
    "B": "Add single-leader replication per shard with configurable async lag; expose read-from-leader vs read-from-follower.",
    "C": "A test harness that shows stale reads under lag and read-your-writes when reading the leader; document the tradeoffs."},
  "cons": {"title": "Why distributed is hard",
    "text": "Reliability metrics, data models, replication, partitioning and consistency are the vocabulary of every distributed data system. Re-read DDIA's chapter summaries for the week.",
    "mastery": "Explain replication lag with a concrete read-your-writes bug, and why partitioning creates hot spots."}},

 {"title": "Distributed Systems II", "days": [
   {"t": "Consensus: Raft & Paxos intuition",
    "concept": "Why consensus is needed (agree on a value despite failures), Raft's leader election and log replication, and Paxos at a high level.",
    "watch": "MIT 6.824 — Raft lectures (Robert Morris); the Raft paper + the raft.github.io visualization.",
    "code": "Implement Raft leader election (terms, votes, election timeouts) for a 3-node cluster."},
   {"t": "Distributed transactions: 2PC & saga",
    "concept": "Two-phase commit and its blocking problem; the saga pattern with compensating actions for long-lived business transactions.",
    "watch": "DDIA ch.9; Hussein Nasser — 'saga pattern'.",
    "code": "Implement a saga for a 3-step booking with compensation when any step fails."},
   {"t": "Event ordering & exactly-once",
    "concept": "Logical clocks (Lamport, vector), idempotency, and why 'exactly-once' is really at-least-once + idempotent processing.",
    "watch": "Kleppmann — logical clocks; search 'exactly once semantics Kafka'.",
    "code": "Make a non-idempotent consumer idempotent with a dedup key so replays don't double-count."},
   {"t": "Fault tolerance & failure modes",
    "concept": "Partial failure, timeouts, retries with backoff + jitter, circuit breakers, and the fallacies of distributed computing.",
    "watch": "Search 'fallacies of distributed computing' and resilience-pattern talks.",
    "code": "Add timeouts, bounded retries with jitter, and a circuit breaker to a flaky client."},
   {"t": "Batch vs stream paradigms",
    "concept": "Bounded (batch) vs unbounded (stream) processing and how they converge (the Dataflow model) — the bridge to the streaming weeks.",
    "watch": "Tyler Akidau — 'The world beyond batch: Streaming 101/102'.",
    "code": "Compute a daily aggregate two ways — as a batch job and as an incremental stream — and compare."},
  ], "build": {"title": "Mini distributed log (Raft-lite)",
    "A": "Build a replicated append-only log across 3 nodes with Raft leader election and log replication (append, commit index).",
    "B": "Handle failure: leader crash -> re-election -> the new leader preserves the committed prefix consistently.",
    "C": "Add a tiny KV state machine on top (the log is the source of truth) and test linearizable reads — a flagship systems portfolio project."},
  "cons": {"title": "Coordination & correctness",
    "text": "Consensus, distributed transactions, ordering/idempotency, fault tolerance and the batch/stream split are the hard core of distributed data. Re-read your Raft-lite and find its remaining edge cases.",
    "mastery": "How does Raft keep a consistent committed log across a leader crash, and why is 'exactly-once' really idempotency?"}},

 {"title": "Data Modeling — Kimball", "days": [
   {"t": "Star schema: facts & dimensions",
    "concept": "The star schema — a central fact table of measurements surrounded by descriptive dimensions — is the analytics default because it maps to how people ask questions.",
    "watch": "Kimball Group resources; search 'star schema / dimensional modeling explained'.",
    "code": "Model a retail-sales star (fact_sales + dim_date/product/store/customer) in SQL/dbt."},
   {"t": "Grain & additivity",
    "concept": "Declare the grain first (one row per what?); classify measures as additive, semi-additive (can't sum across time, e.g. a balance) or non-additive (ratios).",
    "watch": "The Data Warehouse Toolkit (Kimball) ch.1-2 concepts.",
    "code": "Pick a grain for an events fact and label every measure additive / semi / non-additive."},
   {"t": "Slowly changing dimensions (SCD 1/2/3)",
    "concept": "Handling dimension change over time: overwrite (type 1), add a versioned row (type 2), add a column (type 3). SCD2 is the workhorse for 'what was true then?'.",
    "watch": "Search 'slowly changing dimensions type 2 explained'.",
    "code": "Implement an SCD2 dimension (effective_from/to, is_current) with a merge that versions changes."},
   {"t": "Conformed dimensions & the bus matrix",
    "concept": "Shared 'conformed' dimensions let facts from different processes be compared; the Kimball bus matrix is the plan that keeps them conformed.",
    "watch": "Search 'Kimball bus matrix conformed dimensions'.",
    "code": "Draw a bus matrix for 3 business processes and identify the conformed dimensions."},
   {"t": "Data Vault & OBT tradeoffs",
    "concept": "When to use Kimball vs Data Vault (hubs/links/satellites) vs One Big Table (wide, columnar-friendly) — modern warehouse pragmatism.",
    "watch": "Search 'data vault vs kimball' and 'one big table modern data stack'.",
    "code": "Model the same domain as a star and as an OBT; note the query and maintenance tradeoffs."},
  ], "build": {"title": "Model a real domain into a star schema + dbt",
    "A": "Pick a real domain (rides, e-commerce, or your own data) and design the star: declare grain, facts and conformed dimensions.",
    "B": "Implement it in dbt on a warehouse: staging -> dimension models (including one SCD2) -> fact models, with tests.",
    "C": "Add docs + a bus matrix and a couple of analytical queries; ship as a dbt portfolio repo."},
  "cons": {"title": "Modeling for analytics",
    "text": "Grain, facts/dimensions, SCDs, conformed dimensions and the Kimball-vs-Vault-vs-OBT choice are how raw data becomes analyzable. Review your star for grain mistakes.",
    "mastery": "Why declare the grain first, and how does an SCD2 dimension let you answer 'what was true at the time'?"}},

 {"title": "Warehousing & Modeling II", "days": [
   {"t": "Modern warehouse architecture",
    "concept": "Separation of storage and compute (Snowflake/BigQuery/Redshift), MPP execution, and why the cloud warehouse reshaped data teams.",
    "watch": "Search 'how Snowflake works architecture' and 'BigQuery Dremel architecture'.",
    "code": "Load a dataset into a cloud warehouse (or DuckDB as a stand-in) and inspect how it stores and scans."},
   {"t": "Partitioning, clustering & distribution",
    "concept": "The physical-layout knobs — partitioning, clustering/sort keys, distribution — that make scans cheap.",
    "watch": "Search '<your warehouse> clustering and partitioning best practices'.",
    "code": "Partition + cluster a large table on the right columns; measure bytes scanned before vs after."},
   {"t": "Warehouse query optimization",
    "concept": "Partition pruning, avoiding data movement (shuffles), spilling, and reading a warehouse query profile.",
    "watch": "Search 'Snowflake query profile' or 'BigQuery query plan explained'.",
    "code": "Optimize an expensive warehouse query using its profile; cut the bytes scanned."},
   {"t": "Cost & performance tuning",
    "concept": "Warehouses bill for compute and scan — sizing, auto-suspend, materialization and cost observability. FinOps for data.",
    "watch": "Search 'Snowflake cost optimization' or 'BigQuery cost control'.",
    "code": "Find your 3 most expensive queries and cut their cost via materialization, pruning or right-sizing."},
   {"t": "Semantic layers & metrics",
    "concept": "Define each metric once, in a semantic/metrics layer, so every tool agrees — the cure for 'why are our two dashboards different?'.",
    "watch": "Search 'semantic layer explained'; dbt Semantic Layer / MetricFlow docs.",
    "code": "Define 2-3 core business metrics in a semantic layer and query them consistently."},
  ], "build": {"title": "dbt warehouse: staging to marts, tests, docs",
    "A": "Set up dbt on a warehouse (or DuckDB): sources + staging models with tests (not_null, unique, relationships).",
    "B": "Build mart models (the dims + facts from Week 7), incremental where apt, and add a metrics/semantic layer.",
    "C": "Generate docs + lineage, wire a CI check, and publish — a complete analytics-engineering portfolio piece."},
  "cons": {"title": "The warehouse as a product",
    "text": "Architecture, physical layout, query and cost tuning and a semantic layer turn a warehouse into a reliable product. Review your cost wins.",
    "mastery": "How does separating storage and compute change how you tune, and why does a semantic layer prevent metric drift?"}},

 {"title": "Spark Internals I", "days": [
   {"t": "Architecture: driver, executors, managers",
    "concept": "The driver plans, executors do the work, a cluster manager (YARN/K8s/standalone) allocates resources; a job splits into stages at shuffle boundaries, stages into tasks.",
    "watch": "Search 'Spark architecture explained' (Data Savvy / Learning Journal); Spark: The Definitive Guide ch.2-3.",
    "code": "Run a Spark job locally and read the Spark UI: trace jobs -> stages -> tasks."},
   {"t": "RDDs, the DAG & lazy evaluation",
    "concept": "RDDs are the low-level abstraction; transformations are lazy, actions trigger execution, and the scheduler builds a DAG of the work.",
    "watch": "Search 'Spark RDD DAG lazy evaluation'.",
    "code": "Chain several transformations then one action; observe nothing runs until the action, and read the DAG."},
   {"t": "Catalyst optimizer & Tungsten",
    "concept": "The DataFrame API compiles through Catalyst (logical -> optimized -> physical plan) and Tungsten optimizes memory and codegen — why DataFrames beat raw RDDs.",
    "watch": "Search 'Spark Catalyst optimizer explained'; Databricks deep-dive talks.",
    "code": "Call explain() on a DataFrame query; find predicate pushdown and read the physical plan."},
   {"t": "DataFrames vs Datasets vs SQL",
    "concept": "The three structured APIs, when to use each, and why Python users live in DataFrames/SQL (no compile-time Dataset types).",
    "watch": "Spark: The Definitive Guide — Structured APIs.",
    "code": "Express the same transformation in the DataFrame API and in Spark SQL; confirm identical plans."},
   {"t": "Partitions, shuffles, wide vs narrow deps",
    "concept": "Narrow dependencies (map/filter) stay local; wide dependencies (groupBy/join) force a shuffle — the expensive stage boundary.",
    "watch": "Search 'Spark shuffle explained wide narrow dependencies'.",
    "code": "Trigger a shuffle with groupBy; read the stage boundary and shuffle read/write in the UI."},
  ], "build": {"title": "Mini SQL query engine with a cost-based optimizer",
    "A": "Parse a subset of SQL into a logical plan, then execute it over Parquet/CSV with a Volcano-style operator model: scan, filter, project, hash-join, aggregate.",
    "B": "Add a cost-based optimizer — predicate pushdown, projection pruning, and join reordering driven by simple table statistics; print the plan before and after.",
    "C": "Benchmark your engine against DuckDB/Spark on the same queries and write up where a real planner wins — a from-scratch database-internals piece."},
  "cons": {"title": "How Spark executes",
    "text": "Driver/executors, RDDs + the DAG, Catalyst/Tungsten, the API choices and shuffles explain every Spark job. Re-read one of your query plans.",
    "mastery": "Trace a groupBy from code to physical plan to tasks — where does the shuffle happen and why is it costly?"}},

 {"title": "Spark II & PySpark", "days": [
   {"t": "Memory management: execution/storage & spill",
    "concept": "Executor memory splits into execution (shuffles/joins/sorts) and storage (cache); when it runs out Spark spills to disk — the silent killer of job speed.",
    "watch": "Search 'Spark memory management explained'; Databricks tuning talks.",
    "code": "Cache a DataFrame and watch storage memory in the UI, then force a spill and observe the slowdown."},
   {"t": "Joins: broadcast, sort-merge, skew & AQE",
    "concept": "Broadcast small tables, sort-merge large ones; data skew makes one task run forever. Adaptive Query Execution (AQE) fixes many join problems at runtime.",
    "watch": "Search 'Spark join strategies skew AQE'.",
    "code": "Force a broadcast join, cause a skewed sort-merge join, then enable AQE + salting and measure the fix."},
   {"t": "PySpark: UDFs, pandas UDFs, Arrow",
    "concept": "Plain Python UDFs are slow (per-row serialization); pandas/vectorized UDFs with Arrow are far faster — prefer built-ins, then pandas UDFs, then plain UDFs last.",
    "watch": "Search 'PySpark pandas UDF Arrow performance'.",
    "code": "Implement one transform as a plain UDF and as a pandas UDF; benchmark the difference."},
   {"t": "Performance tuning: partitioning & caching",
    "concept": "Right-size partitions (avoid tiny or huge), repartition vs coalesce, cache what you reuse, and tune shuffle partitions.",
    "watch": "Search 'Spark performance tuning partitions shuffle'.",
    "code": "Fix an over-partitioned job (200k tiny tasks) and an under-partitioned one; measure both."},
   {"t": "Debugging via the Spark UI",
    "concept": "The Spark UI is your distributed debugger: long stages, skew (one slow task), spills, GC and shuffle sizes all show up there.",
    "watch": "Search 'reading the Spark UI'; Databricks 'Spark UI' talks.",
    "code": "Take a slow job, diagnose the bottleneck purely from the Spark UI, and prove your fix."},
  ], "build": {"title": "Optimize a Spark job 10x / fix a skewed join",
    "A": "Start from a deliberately slow Spark job (skewed join, bad partitioning, Python UDFs) on a real dataset.",
    "B": "Diagnose from the Spark UI and fix: broadcast/salt the join, right-size partitions, vectorize UDFs, enable AQE — measure at each step.",
    "C": "Portfolio writeup: Spark UI screenshots, each fix, and the 10x before/after."},
  "cons": {"title": "Making Spark fast",
    "text": "Memory/spill, join strategies + skew, UDF choices, partitioning and UI-driven debugging turn a crawling Spark job into a fast one. Re-open your tuned job's UI.",
    "mastery": "Given one task running 10x longer than the rest, what is happening and what are your three fixes?"}},

 {"title": "Databricks & the Lakehouse", "days": [
   {"t": "Lakehouse architecture & Databricks",
    "concept": "The lakehouse merges the lake's cheap open storage with the warehouse's transactions and performance; Databricks is the managed platform around it.",
    "watch": "Search 'lakehouse architecture explained'; a Databricks Lakehouse keynote.",
    "code": "Spin up a free Databricks workspace and run a notebook over a Delta table."},
   {"t": "Delta Lake internals: log & time travel",
    "concept": "Delta adds an ordered transaction log (_delta_log JSON + checkpoints) over Parquet, giving ACID, time travel and schema enforcement on a lake.",
    "watch": "Search 'Delta Lake transaction log explained'; Databricks Delta deep dives.",
    "code": "Make several writes to a Delta table, inspect _delta_log, and query an older version with time travel."},
   {"t": "Delta vs Iceberg vs Hudi",
    "concept": "The three open table formats — how each handles metadata, snapshots and partitioning — and how to choose (engine support, ecosystem).",
    "watch": "Search 'Delta vs Iceberg vs Hudi comparison'.",
    "code": "Create the same table in Delta and Iceberg; compare their metadata layout."},
   {"t": "Medallion: bronze / silver / gold",
    "concept": "The bronze (raw) -> silver (cleaned/conformed) -> gold (business-ready) pattern for organizing a lakehouse.",
    "watch": "Search 'medallion architecture bronze silver gold'.",
    "code": "Build a 3-layer medallion flow for one dataset in Delta."},
   {"t": "Unity Catalog, Photon & governance",
    "concept": "Governance (catalogs, lineage, access control) via Unity Catalog, and the Photon vectorized engine for speed.",
    "watch": "Search 'Databricks Unity Catalog' and 'Photon engine'.",
    "code": "Register tables in a catalog, set a grant, and view lineage."},
  ], "build": {"title": "Medallion lakehouse on Databricks (Delta + DLT)",
    "A": "Ingest a raw source into a bronze Delta table (batch or streaming).",
    "B": "Build silver (validated, deduped, SCD where apt) and gold (aggregated marts) with Delta Live Tables or notebooks + tests.",
    "C": "Add data-quality expectations, a time-travel demo and lineage; publish as a lakehouse portfolio project."},
  "cons": {"title": "The lakehouse paradigm",
    "text": "The transaction log, table-format choices, the medallion pattern and governance turn a pile of Parquet into a real platform. Re-inspect your _delta_log.",
    "mastery": "How does the Delta transaction log give you ACID + time travel on top of immutable Parquet files?"}},

 {"title": "Streaming I — Kafka", "days": [
   {"t": "Kafka architecture: topics, partitions, offsets",
    "concept": "Kafka is a distributed, partitioned, replicated commit log; a topic splits into ordered partitions, each message has an offset — the unit of parallelism and ordering.",
    "watch": "Confluent — Apache Kafka 101; Kafka: The Definitive Guide ch.1-2.",
    "code": "Run Kafka locally, create a topic with N partitions, produce/consume, and watch offsets advance."},
   {"t": "Producers, consumers & consumer groups",
    "concept": "Producers pick partitions by key hashing; consumer groups split partitions across members for scale-out, rebalancing on join/leave.",
    "watch": "Confluent — producers/consumers; 'Kafka consumer groups explained'.",
    "code": "Run 2 consumers in a group over a 4-partition topic; add a third and watch rebalancing."},
   {"t": "Delivery semantics & idempotence",
    "concept": "At-most / at-least / exactly-once; the idempotent producer and transactions, and why consumers still need idempotent processing.",
    "watch": "Search 'Kafka exactly once semantics'.",
    "code": "Show duplicate delivery on retry, then make the consumer idempotent with a dedup store."},
   {"t": "Internals: the log, ISR, retention, compaction",
    "concept": "The segmented on-disk log (page-cache friendly), in-sync replicas (ISR) for durability, and retention vs log compaction.",
    "watch": "Search 'Kafka internals log segments ISR compaction'.",
    "code": "Configure retention + a compacted topic; observe old records expire vs latest-per-key kept."},
   {"t": "Schema Registry & Kafka Connect",
    "concept": "Schema Registry (Avro/Protobuf/JSON schema) for safe evolution, and Kafka Connect for source/sink integration without code.",
    "watch": "Confluent — Schema Registry and Kafka Connect.",
    "code": "Register an Avro schema, produce with it, evolve it compatibly, and sink to a store via Connect."},
  ], "build": {"title": "Real-time pipeline: producer to Kafka to sink",
    "A": "Build a producer that streams events (clicks/txns) into a partitioned topic with keys + an Avro schema.",
    "B": "A consumer group that processes and writes to a sink (Postgres/Delta), idempotently.",
    "C": "Add monitoring (lag, throughput) and a load test; document the ordering + delivery guarantees."},
  "cons": {"title": "Logs as the backbone",
    "text": "Topics/partitions/offsets, consumer groups, delivery semantics, the on-disk log and Schema Registry/Connect make Kafka the nervous system of streaming data. Re-check your consumer lag.",
    "mastery": "How do partitions give both ordering and parallelism, and why is 'exactly-once' really idempotent consumption?"}},

 {"title": "Streaming II — Processing", "days": [
   {"t": "Windows, watermarks & state",
    "concept": "Stream processing needs windows (tumbling/sliding/session), watermarks to bound late data, and managed state — the model behind every engine.",
    "watch": "Tyler Akidau — Streaming 101/102; Flink docs on time & windows.",
    "code": "Compute a 5-minute tumbling count over a stream with a watermark for late events."},
   {"t": "Spark Structured Streaming",
    "concept": "The 'stream as an unbounded table' model, triggers, output modes (append/update/complete), and checkpointing for exactly-once.",
    "watch": "Search 'Spark Structured Streaming tutorial'; Databricks docs.",
    "code": "Read a Kafka stream in Structured Streaming, aggregate a windowed metric, write to Delta with checkpointing."},
   {"t": "Flink concepts & comparison",
    "concept": "Flink's true-streaming, event-time and stateful model vs Spark's micro-batch — when each fits.",
    "watch": "Search 'Flink vs Spark Structured Streaming'.",
    "code": "Sketch the same windowed job in Flink's model; note the event-time/state differences."},
   {"t": "Event time vs processing time, late data",
    "concept": "Event-time (when it happened) vs processing-time (when seen); watermarks + allowed lateness decide when a window closes and how late data is handled.",
    "watch": "Tyler Akidau — Streaming 102 (event time).",
    "code": "Inject out-of-order/late events and show how the watermark + allowed-lateness change results."},
   {"t": "Streaming joins & aggregations",
    "concept": "Stream-stream joins (state + time bounds), stream-static joins, and stateful aggregations — the hard, stateful part.",
    "watch": "Search 'Spark structured streaming joins'.",
    "code": "Join two streams within a time window (impressions to clicks) and emit the matched pairs."},
  ], "build": {"title": "Streaming analytics: Kafka to Structured Streaming to Delta",
    "A": "Consume the Week-12 event stream with Structured Streaming; parse + validate.",
    "B": "Compute windowed, watermarked aggregations with checkpointed state; handle late data explicitly.",
    "C": "Write results to a gold Delta table and a tiny live view; document exactly-once + recovery. A flagship streaming project."},
  "cons": {"title": "Thinking in streams",
    "text": "Windows, watermarks, state, event-time and streaming joins are a genuinely different mental model from batch. Re-read your watermark handling.",
    "mastery": "Why is event-time processing harder than processing-time, and what does a watermark actually decide?"}},

 {"title": "Orchestration", "days": [
   {"t": "DAGs, idempotency & backfills",
    "concept": "Pipelines as DAGs of tasks; idempotent, retryable tasks and backfilling partitioned history are what make schedules reliable.",
    "watch": "Marc Lamberti — Airflow fundamentals; search 'idempotent data pipelines'.",
    "code": "Design a small DAG on paper: tasks, dependencies, and what makes each idempotent."},
   {"t": "Airflow deep: operators, XComs, scheduling",
    "concept": "Operators, the scheduler + executor, XComs for small data passing, sensors, and schedule_interval/catchup.",
    "watch": "Marc Lamberti — Airflow course; the Airflow docs.",
    "code": "Build a real Airflow DAG (extract -> transform -> load) with retries and a sensor."},
   {"t": "Dagster assets / Prefect",
    "concept": "Asset-based orchestration (Dagster software-defined assets) and Prefect's Pythonic flows — a more data-aware model than task-only DAGs.",
    "watch": "Search 'Dagster software-defined assets'; the Dagster docs.",
    "code": "Re-express the same pipeline as Dagster assets and compare to the Airflow DAG."},
   {"t": "Pipeline patterns: incremental & CDC",
    "concept": "Incremental loads (high-water-mark), CDC-driven pipelines, and idempotent upserts/merges.",
    "watch": "Search 'incremental data pipeline patterns'.",
    "code": "Convert a full-refresh load to incremental with a high-water-mark and an idempotent merge."},
   {"t": "Observability & data SLAs",
    "concept": "Freshness/volume/latency SLAs, alerting on failure and lateness, and lineage for impact analysis.",
    "watch": "Search 'data observability SLAs'.",
    "code": "Add task-level alerting + a freshness check to your DAG."},
  ], "build": {"title": "End-to-end orchestrated pipeline (Airflow/Dagster)",
    "A": "Orchestrate a full flow — ingest -> warehouse/lakehouse transform (your dbt project) -> quality checks — as a DAG/asset graph.",
    "B": "Make it incremental + idempotent with retries, backfills and sensors on upstream data.",
    "C": "Add observability (freshness, alerting) and docs; a strong orchestration portfolio piece."},
  "cons": {"title": "Reliable pipelines",
    "text": "DAGs + idempotency, Airflow/Dagster, incremental patterns and SLAs turn scripts-on-cron into dependable production pipelines. Trigger a backfill on your DAG.",
    "mastery": "Why must pipeline tasks be idempotent, and how does a high-water-mark make an incremental load safe to re-run?"}},

 {"title": "dbt & Analytics Engineering", "days": [
   {"t": "dbt fundamentals: models, refs, sources",
    "concept": "dbt turns SQL SELECTs into a DAG of models with ref()/source() and materializations (view/table/incremental), compiled and run against your warehouse.",
    "watch": "dbt Labs — 'dbt Fundamentals' (free course).",
    "code": "Init a dbt project, define sources + a few staging models with ref(), and run it."},
   {"t": "Tests, docs & exposures",
    "concept": "Built-in + custom tests (not_null, unique, relationships, accepted_values), auto-docs, and exposures that track downstream use.",
    "watch": "dbt Fundamentals — testing & docs.",
    "code": "Add schema + data tests to your models and generate the docs site."},
   {"t": "Incremental models & snapshots",
    "concept": "Incremental materialization for big tables (process only new rows) and snapshots for SCD2 history capture.",
    "watch": "Search 'dbt incremental models' and 'dbt snapshots'.",
    "code": "Convert a model to incremental and add a snapshot to track a dimension's history."},
   {"t": "Macros, packages & Jinja",
    "concept": "Jinja templating, reusable macros and packages (dbt-utils) for DRY, testable SQL.",
    "watch": "Search 'dbt macros jinja tutorial'.",
    "code": "Write a macro (e.g. a generic surrogate key) and pull in dbt-utils."},
   {"t": "dbt in production: CI & environments",
    "concept": "dev/prod targets, slim CI (build only changed models), and deploying with a scheduler — the analytics-engineering workflow.",
    "watch": "Search 'dbt slim CI'; dbt deployment docs.",
    "code": "Wire a CI check that builds + tests changed models on a PR."},
  ], "build": {"title": "Reproduce a foundational data-systems paper",
    "A": "Pick a landmark paper (Google MapReduce, Amazon Dynamo's quorums, or Kleppmann's 'Turning the Database Inside-Out') and implement its core mechanism as a working demo.",
    "B": "Design experiments that exercise the paper's claims — partitioning, fault tolerance, or read/write quorums — and measure the trade-offs it describes.",
    "C": "Write a technical report: what the paper claims, what you built, what your measurements show — a research-grade systems artifact."},
  "cons": {"title": "The analytics-engineering mindset",
    "text": "Models + refs, tests + docs, incremental/snapshots, macros and CI are software engineering applied to SQL. Re-read your DAG's lineage.",
    "mastery": "How does dbt's ref() build a DAG, and when do you reach for an incremental model vs a snapshot?"}},

 {"title": "Data Quality & Governance", "days": [
   {"t": "DQ dimensions & testing (Great Expectations)",
    "concept": "Quality dimensions (completeness, validity, uniqueness, freshness, consistency) codified as executable expectations.",
    "watch": "Search 'Great Expectations tutorial'; the GX docs.",
    "code": "Add a Great Expectations suite (types, ranges, null rates) to a dataset and run it in the pipeline."},
   {"t": "Data contracts & schema evolution",
    "concept": "Data contracts as a producer/consumer agreement, and safe schema evolution (backward/forward compatibility).",
    "watch": "Search 'data contracts explained'.",
    "code": "Write a data contract (schema + SLAs) for a source and add a CI check that fails on breaking changes."},
   {"t": "Lineage & catalogs",
    "concept": "Column/table lineage for impact analysis, and a data catalog for discovery/ownership (OpenMetadata/DataHub/Unity).",
    "watch": "Search 'data lineage' and 'data catalog OpenMetadata DataHub'.",
    "code": "Generate lineage for your pipeline and register datasets with owners in a catalog."},
   {"t": "Governance, PII & GDPR",
    "concept": "Classifying/masking PII, access control, and privacy regs (GDPR right-to-erasure) applied to a warehouse/lakehouse.",
    "watch": "Search 'data governance PII masking'; GDPR-for-engineers overviews.",
    "code": "Tag PII columns, add masking for a role, and implement a delete-by-subject flow."},
   {"t": "Observability: freshness, volume, schema",
    "concept": "Automated monitors for freshness, volume/row-count anomalies and schema drift — catching breakage before consumers do.",
    "watch": "Search 'data observability freshness volume schema' (Monte Carlo concepts).",
    "code": "Add freshness + volume + schema-drift monitors with alerting to your pipeline."},
  ], "build": {"title": "Add data quality + lineage to a pipeline",
    "A": "Instrument your orchestrated pipeline with Great Expectations checks at each layer (bronze/silver/gold).",
    "B": "Add a data contract + CI gate on the source, and freshness/volume/schema monitors with alerting.",
    "C": "Generate end-to-end lineage and register everything in a catalog; document the DQ strategy."},
  "cons": {"title": "Trustworthy data",
    "text": "Expectations, contracts, lineage, governance and observability are how data earns trust. Break your source schema and watch the contract catch it.",
    "mastery": "Name the quality dimensions you'd monitor for a critical table, and how a data contract prevents silent breakage."}},

 {"title": "Cloud DE & Advanced Patterns", "days": [
   {"t": "S3, Glue, Athena, EMR",
    "concept": "S3 as the data lake (storage classes, partitioned prefixes), Glue for catalog + ETL, Athena for serverless SQL, EMR for managed Spark.",
    "watch": "Search 'AWS data lake S3 Glue Athena tutorial'.",
    "code": "Land partitioned Parquet in S3, crawl it with Glue, and query it with Athena."},
   {"t": "Kinesis & Lambda for data",
    "concept": "Kinesis for streaming ingest and Lambda for event-driven serverless processing — when serverless beats a cluster.",
    "watch": "Search 'AWS Kinesis Lambda data pipeline'.",
    "code": "Build a Kinesis -> Lambda -> S3 mini streaming ingest."},
   {"t": "Cloud platform fundamentals: IAM, VPC networking, storage & cost",
    "concept": "The transferable cloud layer under every data tool: identity & least-privilege (IAM roles/policies), VPC networking & security groups, storage classes, and cloud cost control — plus Redshift (dist/sort keys, Spectrum) as the warehouse example.",
    "watch": "Search 'AWS IAM explained', 'VPC basics', 'Redshift architecture distribution sort keys'.",
    "code": "Set up a least-privilege IAM role, load data into Redshift Serverless behind a VPC, tune a query with dist/sort keys, and estimate the monthly cost."},
   {"t": "CDC & log-based ingestion",
    "concept": "Change Data Capture with Debezium reading the DB WAL/binlog into Kafka — low-latency, low-load replication of changes.",
    "watch": "Search 'Debezium CDC tutorial'; Confluent CDC talks.",
    "code": "Set up Debezium on Postgres, stream changes into Kafka, and materialize them in a sink."},
   {"t": "Lambda vs Kappa; reverse ETL",
    "concept": "Lambda (batch + speed layers) vs Kappa (stream-only) architectures, and reverse ETL to push warehouse data into operational tools.",
    "watch": "Search 'lambda vs kappa architecture' and 'reverse ETL explained'.",
    "code": "Diagram your platform as Kappa; sketch a reverse-ETL sync of a gold table to an app."},
  ], "build": {"title": "Serverless data pipeline (S3 + Glue + Athena)",
    "A": "Ingest raw data to S3 (partitioned), catalog with Glue, transform with a Glue/Spark job to Parquet.",
    "B": "Expose it via Athena with partition pruning; add a Kinesis -> Lambda path for near-real-time.",
    "C": "Add least-privilege IAM, cost estimates and a preview of IaC; document the serverless design."},
  "cons": {"title": "DE in the cloud",
    "text": "S3/Glue/Athena/EMR, Kinesis/Lambda, Redshift, CDC and the Lambda/Kappa choice are the cloud toolkit that ties the phase together. Review your IAM + cost.",
    "mastery": "When do you pick serverless (Athena/Lambda) over a cluster (EMR/Redshift), and what does CDC buy you over batch pulls?"}},

 {"title": "DE Capstone", "days": [
   {"t": "Design the platform & data contracts",
    "concept": "Scope a real end-to-end platform: sources, SLAs, the medallion layers, and data contracts at the boundaries.",
    "watch": "Revisit DDIA ch.1 and your Kimball/dbt notes as design references.",
    "code": "Write the design doc: an architecture diagram, the data contracts, and the SLAs you'll hit."},
   {"t": "Ingestion & the bronze layer",
    "concept": "Batch + streaming ingestion into raw/bronze with schema capture and idempotent, replay-safe landing.",
    "watch": "Reuse your Kafka + ingestion-library patterns.",
    "code": "Build ingestion into bronze (Kafka/CDC + batch) with validation and replay-safety."},
   {"t": "Transformation & modeling (silver/gold)",
    "concept": "Clean/conform to silver and model business marts (dims + facts, SCD2) to gold with dbt on your lakehouse/warehouse.",
    "watch": "Reuse your dbt + dimensional-modeling work.",
    "code": "Build silver + gold with dbt: staging, dims (incl. SCD2), facts, tests."},
   {"t": "Orchestration, quality & monitoring",
    "concept": "Orchestrate the flow (Airflow/Dagster), add DQ (Great Expectations) and observability (freshness/volume) end to end.",
    "watch": "Reuse your orchestration + DQ patterns.",
    "code": "Wire the whole DAG with retries/backfills, quality gates and alerting."},
   {"t": "Ship, document & diagram",
    "concept": "Package, write the README/architecture doc, add lineage, and prepare the portfolio narrative.",
    "watch": "Study a couple of strong data-platform case studies for structure.",
    "code": "Finalize docs + lineage + a diagram and record a short walkthrough."},
  ], "build": {"title": "Data-platform capstone — build & ship",
    "A": "Assemble the full platform: ingest (batch + streaming/CDC) -> bronze -> silver -> gold, orchestrated.",
    "B": "Add data quality, observability and cost/perf tuning across the stack; make it reproducible.",
    "C": "Ship it: repo, architecture diagram, README and a demo — your flagship Data Engineering portfolio project."},
  "cons": {"title": "Phase 1 review",
    "text": "You've built the machine beneath the data and a full platform on top. Step back: what would you do differently, and which pieces will the ML and AI phases plug into?",
    "mastery": "Give the 3-minute architecture tour of your platform: how data flows from source to gold, and where quality, orchestration and cost are handled."}},

 # ============================ PHASE 2 — DATA SCIENCE & ML ===================
 {"title": "Math I — Linear Algebra", "days": [
   {"t": "Vectors, matrices & operations",
    "concept": "Vectors as points/directions, matrices as linear maps; dot products and matrix multiply as composition of transforms — the language of ML.",
    "watch": "3Blue1Brown — Essence of Linear Algebra (ch.1-4).",
    "code": "Implement vector/matrix ops in NumPy from the definitions and verify against np.dot."},
   {"t": "Matrix decompositions: eigen & SVD",
    "concept": "Eigenvectors/values (directions a transform only scales) and SVD (any matrix = rotate-scale-rotate) — the engine behind PCA, recommenders and compression.",
    "watch": "3Blue1Brown — eigenvectors; StatQuest — SVD/PCA.",
    "code": "Compute eigen-decomposition and SVD with NumPy; reconstruct a matrix from its top-k singular values."},
   {"t": "Vector spaces, projections & norms",
    "concept": "Span, basis, linear independence, projections (least squares) and L1/L2 norms — geometry that underlies regression and regularization.",
    "watch": "3Blue1Brown — Essence of Linear Algebra (span/basis/dot).",
    "code": "Project a vector onto a subspace and solve a least-squares fit via the normal equations."},
   {"t": "PCA from linear algebra",
    "concept": "PCA is the eigen-decomposition of the covariance (or SVD of centered data); it finds the axes of maximum variance for dimensionality reduction.",
    "watch": "StatQuest — PCA step-by-step.",
    "code": "Implement PCA from scratch (center, covariance, eigen/SVD, project) and compare to sklearn."},
   {"t": "NumPy for linear algebra",
    "concept": "Broadcasting, vectorized linear algebra (np.linalg), and why staying in NumPy instead of Python loops is 100x faster.",
    "watch": "Jake VanderPlas — Python Data Science Handbook (NumPy).",
    "code": "Vectorize a from-scratch algorithm (e.g. a k-means step) using broadcasting; benchmark vs loops."},
  ], "build": {"title": "Implement PCA from scratch; compress images",
    "A": "Build PCA from scratch (center, covariance/SVD, project, reconstruct) and validate against sklearn.",
    "B": "Apply it to image compression: reduce to k components, reconstruct, and plot explained-variance vs quality.",
    "C": "Write up the linear-algebra intuition with visuals — a clean portfolio notebook."},
  "cons": {"title": "Linear-algebra intuition",
    "text": "Vectors/matrices, eigen/SVD, projections/norms and PCA are the geometric core of ML. Re-derive PCA from the covariance in your head.",
    "mastery": "What does SVD do geometrically, and why is PCA just an eigen/SVD problem on centered data?"}},

 {"title": "Math II — Calculus & Optimization", "days": [
   {"t": "Derivatives, gradients & the chain rule",
    "concept": "Derivatives as sensitivity, the gradient as the direction of steepest ascent, and the chain rule that makes backprop possible.",
    "watch": "3Blue1Brown — Essence of Calculus (ch.1-4).",
    "code": "Implement numeric and analytic gradients for a small function and check they match."},
   {"t": "Gradient descent & its variants",
    "concept": "Following the negative gradient to minimize loss; learning rate, batch vs stochastic vs mini-batch, momentum and Adam.",
    "watch": "3Blue1Brown — gradient descent (Neural Networks ch.2); StatQuest — gradient descent.",
    "code": "Implement batch and mini-batch gradient descent for linear regression from scratch."},
   {"t": "Convexity & Lagrange multipliers",
    "concept": "Convex problems have one global minimum (why linear/logistic regression are nice); Lagrange multipliers handle constrained optimization.",
    "watch": "Search 'convex optimization intuition'; Khan Academy — Lagrange multipliers.",
    "code": "Minimize a convex function with gradient descent, verify the global optimum, and solve a simple constrained problem."},
   {"t": "Backprop as the chain rule (preview)",
    "concept": "Backpropagation is just the chain rule applied over a computation graph — a preview you'll build for real in the AI phase.",
    "watch": "3Blue1Brown — backpropagation; Andrej Karpathy — micrograd (intro).",
    "code": "Hand-derive gradients for a 2-layer function via the chain rule and verify them numerically."},
   {"t": "Optimization in practice",
    "concept": "Learning-rate schedules, initialization, local minima vs saddle points, and diagnosing bad optimization.",
    "watch": "Search 'optimization tricks deep learning'; Andrew Ng — optimization lectures.",
    "code": "Visualize gradient descent on a 2D loss surface with different learning rates."},
  ], "build": {"title": "Gradient-descent visualizer over loss landscapes",
    "A": "Implement gradient descent (with momentum + Adam) from scratch on 2D test functions (bowl, saddle, Rosenbrock).",
    "B": "Animate the optimizer's path over contour plots; show how LR, momentum and the surface interact.",
    "C": "Extend it to fit a small model and visualize the loss dropping — a great intuition-building portfolio piece."},
  "cons": {"title": "How models learn",
    "text": "Gradients, gradient descent, convexity and the chain rule are the machinery of every model that learns. Re-watch backprop with your visualizer open.",
    "mastery": "Why does gradient descent work, and what do momentum and Adam fix about plain SGD?"}},

 {"title": "Math III — Probability & Statistics", "days": [
   {"t": "Probability & distributions",
    "concept": "Random variables, PMF/PDF, expectation/variance, and the key distributions (Bernoulli, binomial, normal, Poisson) you meet everywhere.",
    "watch": "StatQuest — probability & distributions; Khan Academy — probability.",
    "code": "Simulate distributions in NumPy, plot histograms vs the theoretical PDF, and estimate mean/variance."},
   {"t": "Bayes & conditional probability",
    "concept": "Conditional probability, Bayes' theorem, prior/likelihood/posterior, and the base-rate fallacy that fools everyone.",
    "watch": "3Blue1Brown — Bayes' theorem; StatQuest — Bayes.",
    "code": "Compute a posterior for a classic problem (medical test) and show how the base rate dominates."},
   {"t": "Descriptive & inferential statistics",
    "concept": "Sampling, the central limit theorem, standard error, and going from a sample to a population claim.",
    "watch": "StatQuest — CLT and standard error; Khan Academy — inference.",
    "code": "Demonstrate the CLT by sampling means from a non-normal distribution and watching them go normal."},
   {"t": "Hypothesis testing, CIs & p-values",
    "concept": "Null/alternative hypotheses, p-values (and what they are NOT), confidence intervals and t-tests.",
    "watch": "StatQuest — p-values, hypothesis testing, confidence intervals.",
    "code": "Run a t-test + bootstrap confidence interval on a real dataset and interpret the p-value correctly."},
   {"t": "A/B testing & experimental design",
    "concept": "Designing experiments, power/sample size, and pitfalls (peeking, multiple comparisons) — the DS bread and butter.",
    "watch": "Search 'A/B testing statistics'; StatQuest — power analysis.",
    "code": "Design and analyze an A/B test: compute the required sample size, then analyze simulated results."},
  ], "build": {"title": "A/B-test analysis toolkit + a simulation",
    "A": "Build a reusable A/B toolkit: a sample-size/power calculator, t-test + bootstrap CIs, and effect-size reporting.",
    "B": "Run a Monte Carlo simulation showing false-positive rates under peeking and multiple comparisons.",
    "C": "Package + document it with a clear 'how to run a valid A/B test' guide — a standout portfolio piece for DS roles."},
  "cons": {"title": "Reasoning under uncertainty",
    "text": "Distributions, Bayes, inference, hypothesis testing and A/B design are how you make honest claims from data. Re-read your p-value interpretation and get it exactly right.",
    "mastery": "What does a p-value actually mean (and not mean), and why does peeking at an A/B test inflate false positives?"}},

 {"title": "Data Analysis & Wrangling", "days": [
   {"t": "NumPy deep: broadcasting & vectorization",
    "concept": "The ndarray, broadcasting rules, fancy indexing and vectorized thinking — the substrate under pandas and every ML library.",
    "watch": "Jake VanderPlas — Python Data Science Handbook (NumPy); Keith Galli — NumPy.",
    "code": "Solve 10 array problems using only vectorized ops (no Python loops)."},
   {"t": "Pandas mastery: groupby, merge, reshape",
    "concept": "Indexing/selection, groupby-apply, joins/merges, pivot/melt and time-based ops — the daily driver of data work.",
    "watch": "Wes McKinney — Python for Data Analysis; Keith Galli — pandas.",
    "code": "Do a full wrangle: load messy data, groupby-aggregate, merge sources, pivot to a report."},
   {"t": "EDA methodology",
    "concept": "A disciplined EDA loop: shape/types, distributions, missingness, correlations, outliers, and forming hypotheses before modeling.",
    "watch": "Search 'exploratory data analysis methodology'; strong Kaggle EDA notebooks.",
    "code": "Run a structured EDA on a real dataset and write down 5 hypotheses it surfaces."},
   {"t": "Cleaning, missing data & outliers",
    "concept": "Missing-data mechanisms (MCAR/MAR/MNAR), imputation strategies, outlier detection, and how cleaning choices bias results.",
    "watch": "StatQuest — missing data; search 'handling missing data imputation'.",
    "code": "Handle missing values three ways (drop, mean, model-based) and compare the downstream impact."},
   {"t": "Visualization: matplotlib, seaborn, plotly",
    "concept": "Choosing the right chart, matplotlib fundamentals, seaborn for stats plots, plotly for interactivity, and honest visualization.",
    "watch": "Corey Schafer — matplotlib; the seaborn docs.",
    "code": "Build a small dashboard of 4 well-chosen charts that tell a story about your dataset."},
  ], "build": {"title": "Reproduce a landmark ML paper (ablation + report)",
    "A": "Choose an influential ML paper (Dropout, Batch Normalization, or a RecSys paper like Wide & Deep / DeepFM) and reproduce its central result from the description.",
    "B": "Run a proper ablation study — turn the mechanism on and off, vary the key hyperparameters — and quantify its effect against clean, honest baselines.",
    "C": "Write a technical report with figures and a limitations section — a research-grade artifact showing you can read, implement, and critique a paper."},
  "cons": {"title": "Letting the data speak",
    "text": "NumPy, pandas, EDA method, cleaning and visualization turn raw files into understanding — the step before any model. Re-read your EDA's conclusions.",
    "mastery": "What's your EDA checklist before modeling, and how can a naive missing-data fix bias your results?"}},

 {"title": "Classical ML I — Supervised", "days": [
   {"t": "The ML framework: bias-variance",
    "concept": "Train/validation/test splits, generalization, overfitting/underfitting, and the bias-variance tradeoff that governs everything.",
    "watch": "StatQuest — bias/variance; Andrew Ng — Machine Learning Specialization (course 1).",
    "code": "Fit models of increasing complexity and plot train vs validation error to see the tradeoff."},
   {"t": "Linear regression from scratch",
    "concept": "The model, the loss (MSE), the closed-form normal equation, and gradient descent — regression from first principles.",
    "watch": "StatQuest — linear regression; Andrew Ng — linear regression.",
    "code": "Implement linear regression from scratch (normal equation and gradient descent) and match sklearn."},
   {"t": "Logistic regression & classification",
    "concept": "The sigmoid, log-loss, decision boundaries, and why it's classification despite the name.",
    "watch": "StatQuest — logistic regression; Andrew Ng — classification.",
    "code": "Implement logistic regression from scratch with gradient descent on a binary dataset."},
   {"t": "Regularization: L1/L2 & elastic net",
    "concept": "Ridge (L2), Lasso (L1, sparsity/feature selection) and elastic net; how the penalty trades bias for lower variance.",
    "watch": "StatQuest — ridge, lasso, elastic net.",
    "code": "Add L1 and L2 penalties to your regression; watch coefficients shrink and features drop out."},
   {"t": "The sklearn API & pipelines",
    "concept": "fit/predict/transform, Pipeline + ColumnTransformer, cross-validation, and avoiding leakage by preprocessing inside the pipeline.",
    "watch": "Search 'sklearn pipeline tutorial'; the sklearn user guide.",
    "code": "Build a leak-free sklearn Pipeline (impute -> scale -> encode -> model) with cross-validation."},
  ], "build": {"title": "Linear & logistic regression from scratch",
    "A": "Implement linear and logistic regression from scratch (loss, gradients, training loop, regularization).",
    "B": "Validate against sklearn on real datasets; add L1/L2 and plot the effect.",
    "C": "Write it up as a 'ML from first principles' notebook — it proves you understand what the library hides."},
  "cons": {"title": "The supervised-learning core",
    "text": "The train/val/test frame, bias-variance, the two workhorse models, regularization and the sklearn workflow are the foundation of applied ML. Re-derive the logistic gradient.",
    "mastery": "Explain the bias-variance tradeoff with a concrete overfit example, and what L1 does that L2 doesn't."}},

 {"title": "Classical ML II — Trees & Ensembles", "days": [
   {"t": "Decision trees",
    "concept": "Recursive splits on impurity (Gini/entropy), how trees overfit, and pruning/depth control.",
    "watch": "StatQuest — decision trees.",
    "code": "Fit a decision tree, visualize it, and show how max_depth controls overfitting."},
   {"t": "Bagging & random forests",
    "concept": "Bootstrap aggregating to reduce variance; random forests add feature randomness for decorrelated trees.",
    "watch": "StatQuest — random forests and bagging.",
    "code": "Build a random forest, compare it to a single tree, and inspect out-of-bag error."},
   {"t": "Gradient boosting (XGBoost, LightGBM)",
    "concept": "Boosting fits trees to residuals sequentially; XGBoost/LightGBM are the tabular workhorses that win competitions.",
    "watch": "StatQuest — gradient boost and XGBoost.",
    "code": "Train XGBoost and LightGBM on a tabular problem; tune key params and beat the random forest."},
   {"t": "Feature importance & SHAP",
    "concept": "Impurity vs permutation importance (and their traps), and SHAP for consistent per-prediction explanations.",
    "watch": "Search 'SHAP values explained'; StatQuest — SHAP.",
    "code": "Compute SHAP values for your boosted model and explain a few individual predictions."},
   {"t": "Hyperparameter tuning",
    "concept": "Grid vs random vs Bayesian search (Optuna), cross-validated tuning, and not overfitting the validation set.",
    "watch": "Search 'Optuna tutorial' and 'hyperparameter tuning strategies'.",
    "code": "Tune your XGBoost model with Optuna + cross-validation and report the honest gain."},
  ], "build": {"title": "Gradient boosting from scratch, then benchmarked",
    "A": "Implement gradient-boosted decision trees from scratch in NumPy (regression + classification): greedy trees, shrinkage, subsampling — the core of XGBoost.",
    "B": "On a real tabular problem, match your implementation against XGBoost/LightGBM tuned with Optuna, and explain predictions with SHAP.",
    "C": "Write up the accuracy/speed gap and what the production libraries add (histograms, regularization, parallelism) — from-scratch depth plus applied ML judgment."},
  "cons": {"title": "Ensembles in practice",
    "text": "Trees, bagging/forests, boosting, importance/SHAP and tuning are the tabular-ML toolkit that wins in practice. Re-read your SHAP explanations.",
    "mastery": "Why does bagging reduce variance while boosting reduces bias, and what makes gradient boosting so strong on tabular data?"}},

 {"title": "Classical ML III — More Methods", "days": [
   {"t": "SVMs & kernels",
    "concept": "Maximum-margin classifiers and the kernel trick for non-linear boundaries without explicit high-dim features.",
    "watch": "StatQuest — SVMs and kernels.",
    "code": "Train an SVM with linear and RBF kernels; visualize the decision boundary and margins."},
   {"t": "kNN & Naive Bayes",
    "concept": "Instance-based kNN (lazy, distance-driven) and Naive Bayes (fast, probabilistic, strong on text).",
    "watch": "StatQuest — kNN and Naive Bayes.",
    "code": "Implement kNN from scratch and use Naive Bayes for a text-classification baseline."},
   {"t": "Clustering: k-means, DBSCAN, hierarchical",
    "concept": "Unsupervised grouping: k-means (centroids), DBSCAN (density, finds noise), hierarchical (dendrograms) — and picking k.",
    "watch": "StatQuest — k-means and hierarchical clustering.",
    "code": "Cluster a dataset three ways; compare and use the elbow/silhouette to pick k."},
   {"t": "Dimensionality reduction: t-SNE & UMAP",
    "concept": "Non-linear embeddings for visualization (t-SNE, UMAP) vs PCA, and how to read (and misread) these plots.",
    "watch": "StatQuest — t-SNE; search 'UMAP explained'.",
    "code": "Embed a high-dim dataset with PCA, t-SNE and UMAP; compare the 2D structure."},
   {"t": "Anomaly detection",
    "concept": "Statistical, isolation-forest and density-based anomaly detection, and the imbalance/evaluation challenge.",
    "watch": "Search 'anomaly detection isolation forest'.",
    "code": "Detect anomalies with an isolation forest and evaluate with precision@k on a labeled set."},
  ], "build": {"title": "Dimensionality reduction from scratch (t-SNE / UMAP)",
    "A": "Implement a nonlinear embedding method from scratch — t-SNE or UMAP — including the neighbor graph, the objective, and gradient-based optimization.",
    "B": "Apply it to a real high-dimensional dataset; compare your embeddings against scikit-learn's and against PCA, and measure neighborhood preservation quantitatively.",
    "C": "Write up how the algorithm works and where it distorts structure — a piece that shows you understand representation learning, not just call .fit()."},
  "cons": {"title": "The algorithm zoo",
    "text": "SVMs/kernels, kNN/Naive Bayes, clustering, dimensionality reduction and anomaly detection round out classical ML. Re-read your t-SNE plot critically.",
    "mastery": "When would you reach for DBSCAN over k-means, and why can a t-SNE plot mislead you?"}},

 {"title": "Feature Engineering", "days": [
   {"t": "Feature-engineering techniques",
    "concept": "Transformations, interactions, binning and aggregations — and why features often matter more than the model.",
    "watch": "Search 'feature engineering for machine learning'; the Kaggle Feature Engineering course.",
    "code": "Engineer 10 features for a tabular problem and measure each one's marginal lift."},
   {"t": "Categorical encoding & embeddings",
    "concept": "One-hot, ordinal, target/mean encoding (with leakage care), and learned embeddings for high-cardinality categories.",
    "watch": "Search 'categorical encoding techniques' and 'target encoding leakage'.",
    "code": "Compare one-hot vs target encoding (done inside CV to avoid leakage) on a high-cardinality feature."},
   {"t": "Scaling & transformations",
    "concept": "Standardization vs min-max, log/Box-Cox for skew, and when scaling matters (SVM/kNN/NN) vs not (trees).",
    "watch": "StatQuest — normalization; search 'feature scaling when to use'.",
    "code": "Show how scaling changes kNN/SVM but not a tree, and apply a log transform to a skewed feature."},
   {"t": "Time-series features",
    "concept": "Lags, rolling windows, date/calendar features, and avoiding leakage from the future in temporal data.",
    "watch": "Search 'time series feature engineering'.",
    "code": "Build lag + rolling features for a time series with a strict no-future-leakage split."},
   {"t": "Feature stores (Feast)",
    "concept": "Why feature stores exist (training/serving consistency, reuse, point-in-time correctness) and how Feast works.",
    "watch": "Search 'feature store explained'; the Feast quickstart.",
    "code": "Define a couple of features in Feast, materialize them, and fetch a point-in-time training set."},
  ], "build": {"title": "Feature pipeline + a feature store",
    "A": "Build a reusable feature pipeline (transformations, encoding, scaling) as sklearn transformers, leak-free.",
    "B": "Register features in Feast with point-in-time correctness; serve them for training and inference.",
    "C": "Document the training/serving-skew problem the feature store solves — a strong MLOps-adjacent portfolio piece."},
  "cons": {"title": "Features make or break models",
    "text": "Techniques, encoding, scaling, time-series features and feature stores are where most model performance actually comes from. Re-check one feature for leakage.",
    "mastery": "What is training/serving skew, and how does target encoding leak if you're not careful?"}},

 {"title": "Model Evaluation", "days": [
   {"t": "Metrics: classification, regression, ranking",
    "concept": "Accuracy's traps, precision/recall/F1, ROC-AUC vs PR-AUC, regression metrics (RMSE/MAE/R2), and ranking metrics (NDCG).",
    "watch": "StatQuest — confusion matrix, ROC/AUC, precision/recall.",
    "code": "Compute the full metric suite for a model and explain which metric fits the business goal."},
   {"t": "Cross-validation strategies",
    "concept": "k-fold, stratified, group and time-series CV — matching the split to the data for an honest estimate.",
    "watch": "StatQuest — cross validation; search 'time series cross validation'.",
    "code": "Compare naive k-fold vs grouped/time-series CV on data with groups/time; show the leakage gap."},
   {"t": "ROC/PR curves & calibration",
    "concept": "Threshold choice via ROC/PR, and probability calibration (reliability curves, Platt/isotonic) so scores mean something.",
    "watch": "StatQuest — ROC/AUC; search 'model calibration explained'.",
    "code": "Plot ROC + PR curves, pick a threshold for a cost tradeoff, and calibrate the probabilities."},
   {"t": "Handling class imbalance",
    "concept": "Resampling (SMOTE), class weights, threshold moving, and why accuracy lies on imbalanced data.",
    "watch": "Search 'handling imbalanced data' and 'SMOTE explained'.",
    "code": "Take an imbalanced problem; compare class weights, SMOTE and threshold tuning by PR-AUC."},
   {"t": "Interpretability",
    "concept": "Global (feature importance, PDP) vs local (SHAP, LIME) explanations, and communicating model behavior to stakeholders.",
    "watch": "Search 'SHAP vs LIME' and 'partial dependence plots'.",
    "code": "Explain your model globally (PDP) and locally (SHAP), then write a one-paragraph stakeholder summary."},
  ], "build": {"title": "Evaluation + interpretability toolkit",
    "A": "Build a reusable evaluation module: the metric suite, the right CV, ROC/PR + calibration plots.",
    "B": "Add interpretability: PDPs, SHAP, and an imbalance-aware threshold selector.",
    "C": "Run it on a real model and produce a model card — a portfolio piece that shows rigor, not just accuracy."},
  "cons": {"title": "Trusting your metrics",
    "text": "Metrics, CV, calibration, imbalance handling and interpretability are how you know a model is actually good. Re-read your model card.",
    "mastery": "Why can accuracy be 99% and useless, and when is PR-AUC the honest metric over ROC-AUC?"}},

 {"title": "Time Series & Forecasting", "days": [
   {"t": "TS concepts: stationarity & autocorrelation",
    "concept": "Trend/seasonality/noise decomposition, stationarity, and autocorrelation (ACF/PACF) — the vocabulary of time series.",
    "watch": "Search 'time series analysis stationarity ACF PACF'.",
    "code": "Decompose a series, test stationarity (ADF), and plot ACF/PACF."},
   {"t": "Classical: ARIMA & ETS",
    "concept": "AR, MA, ARIMA and exponential smoothing (ETS); when classical models still beat ML on small clean series.",
    "watch": "Search 'ARIMA explained' and 'exponential smoothing'.",
    "code": "Fit ARIMA and ETS to a series, diagnose residuals, and forecast with intervals."},
   {"t": "Prophet & modern methods",
    "concept": "Prophet's decomposable model, and ML approaches (gradient boosting on lag features) for complex series.",
    "watch": "Search 'Facebook Prophet tutorial'.",
    "code": "Forecast the same series with Prophet and a gradient-boosting-on-lags model, then compare."},
   {"t": "Feature-based forecasting",
    "concept": "Reframing forecasting as supervised learning with lag/rolling/calendar features, and multi-step strategies.",
    "watch": "Search 'time series forecasting as supervised learning'.",
    "code": "Build a feature-based forecaster with proper temporal CV and compare it to ARIMA."},
   {"t": "Evaluation & backtesting",
    "concept": "Rolling-origin backtesting, horizon-aware metrics (MAPE/sMAPE/MASE), and why a random split is wrong here.",
    "watch": "Search 'time series cross validation backtesting'.",
    "code": "Backtest your models with rolling-origin evaluation and report horizon-wise error."},
  ], "build": {"title": "Demand / energy forecasting with backtesting",
    "A": "Pick a real forecasting problem (demand, energy, traffic); EDA + decomposition + features.",
    "B": "Build classical (ARIMA/Prophet) and ML (boosting-on-lags) forecasters; backtest with rolling origin.",
    "C": "Report which wins by horizon with proper metrics + intervals — a rigorous forecasting portfolio piece."},
  "cons": {"title": "Predicting the future",
    "text": "Stationarity/autocorrelation, classical models, Prophet/ML, feature-based framing and backtesting are the forecasting toolkit. Re-read your backtest for leakage.",
    "mastery": "Why is a random train/test split wrong for time series, and how does rolling-origin backtesting fix it?"}},

 {"title": "Recommender Systems", "days": [
   {"t": "Collaborative filtering",
    "concept": "User-user and item-item CF from the interaction matrix; the 'people like you also liked' intuition.",
    "watch": "Search 'collaborative filtering explained'; Andrew Ng — recommenders.",
    "code": "Implement item-item collaborative filtering from a ratings matrix."},
   {"t": "Matrix factorization",
    "concept": "Learning latent user/item factors (SVD-style, ALS) to fill the sparse matrix — the classic recsys workhorse.",
    "watch": "Search 'matrix factorization recommender systems'.",
    "code": "Implement matrix factorization with gradient descent on a ratings dataset."},
   {"t": "Content-based & hybrid",
    "concept": "Using item features (and embeddings) for cold-start, and hybrid systems that blend CF + content.",
    "watch": "Search 'content based vs collaborative filtering hybrid'.",
    "code": "Add a content-based model and blend it with your CF into a hybrid recommender."},
   {"t": "Implicit feedback & ranking",
    "concept": "Clicks/views (implicit) vs ratings, ranking losses (BPR), and the top-N recommendation framing.",
    "watch": "Search 'implicit feedback recommender BPR'.",
    "code": "Train an implicit-feedback ranker and generate top-N recommendations."},
   {"t": "Recommender evaluation",
    "concept": "Offline metrics (precision@k, recall@k, NDCG, MAP), the offline/online gap, and coverage/diversity.",
    "watch": "Search 'recommender system evaluation metrics'.",
    "code": "Evaluate your recommenders with precision@k + NDCG and compare them fairly."},
  ], "build": {"title": "End-to-end recommender system",
    "A": "Pick a real interactions dataset (MovieLens etc.) and build CF + matrix-factorization baselines.",
    "B": "Add content features for cold-start and a hybrid ranker for implicit feedback.",
    "C": "Evaluate with ranking metrics and serve top-N via a small API — a portfolio recsys project."},
  "cons": {"title": "Systems that personalize",
    "text": "Collaborative filtering, matrix factorization, content/hybrid, implicit ranking and evaluation are the recommender toolkit. Re-read your offline-vs-online caveat.",
    "mastery": "How does matrix factorization fill a sparse ratings matrix, and why can offline metrics mislead you about a live recommender?"}},

 {"title": "MLOps I — Productionizing", "days": [
   {"t": "Containers & Docker for ML — a just-in-time primer",
    "concept": "Just enough containers to productionize ML now: images, layers, Dockerfiles, volumes, and reproducible environments — a bridge to the deep Containers/Kubernetes track later. (Full ML system design is its own week soon.)",
    "watch": "Docker in 100 Seconds; Docker for Data Science/ML tutorials.",
    "code": "Containerize a training + inference script with a slim multi-stage Dockerfile; build, run, and push the image to a registry."},
   {"t": "Experiment tracking (MLflow, W&B)",
    "concept": "Tracking params/metrics/artifacts for reproducibility and comparison instead of losing results in notebooks.",
    "watch": "MLflow quickstart; Weights & Biases tutorials.",
    "code": "Instrument a training script with MLflow (params, metrics, model) and compare runs in the UI."},
   {"t": "Model registry & versioning",
    "concept": "Registering models with stages (staging/production), versioning, and promoting the right model to serving.",
    "watch": "MLflow Model Registry docs.",
    "code": "Register your model, version it, and promote a version to 'production' via the registry."},
   {"t": "Reproducible ML pipelines",
    "concept": "Turning notebooks into reproducible pipelines (scripts, configs, seeds, environments) so results are rebuildable.",
    "watch": "Made With ML — reproducibility; search 'reproducible ML pipeline'.",
    "code": "Refactor a notebook into a config-driven, seeded training pipeline that runs end to end."},
   {"t": "Data & model versioning (DVC)",
    "concept": "Versioning datasets and models alongside code with DVC, so an experiment is fully reconstructable.",
    "watch": "DVC docs/tutorial.",
    "code": "Version a dataset + model with DVC and reproduce a past experiment from a git+dvc checkout."},
  ], "build": {"title": "MLflow-tracked training pipeline",
    "A": "Build a reproducible, config-driven training pipeline (data -> features -> train -> evaluate).",
    "B": "Instrument it with MLflow (params/metrics/artifacts) + DVC data versioning, and register the best model.",
    "C": "Document how to reproduce any run from git + dvc — a portfolio MLOps foundation piece."},
  "cons": {"title": "From notebook to system",
    "text": "System design, experiment tracking, registry/versioning, reproducibility and DVC turn ad-hoc modeling into engineering. Reproduce an old run to prove it works.",
    "mastery": "What makes an ML experiment reproducible, and what does a model registry give you that a folder of pickles doesn't?"}},

 {"title": "MLOps II — Serving & Monitoring", "days": [
   {"t": "Serving (FastAPI, BentoML)",
    "concept": "Wrapping a model in an API, request/response schemas, and packaging with BentoML for standardized serving.",
    "watch": "Search 'FastAPI ML model serving'; the BentoML docs.",
    "code": "Serve your model behind a FastAPI endpoint with a typed request/response and validation."},
   {"t": "Batch vs online inference",
    "concept": "Precomputed batch predictions vs low-latency online inference, and the latency/cost/freshness tradeoffs.",
    "watch": "Chip Huyen — batch vs online; search 'batch vs online inference'.",
    "code": "Implement both a batch scoring job and an online endpoint for the same model; compare latency."},
   {"t": "Monitoring: drift & performance",
    "concept": "Data drift, concept drift, and monitoring live performance/latency — models decay silently without it.",
    "watch": "Search 'ML model monitoring data drift'; the Evidently AI docs.",
    "code": "Add drift detection (e.g. Evidently) + latency/throughput metrics to your service."},
   {"t": "Shadow & A/B deployment",
    "concept": "Rolling out models safely: shadow (mirror traffic), canary, and A/B testing model versions in production.",
    "watch": "Search 'shadow deployment canary ML'.",
    "code": "Add a shadow-mode path that scores with a new model without affecting responses; log the comparison."},
   {"t": "CI/CD for ML",
    "concept": "Automated testing, model-validation gates, and deployment pipelines (CML/GitHub Actions) for models.",
    "watch": "Search 'CI/CD for machine learning'; Made With ML — CI/CD.",
    "code": "Wire a CI pipeline that tests, validates the model against a threshold, and deploys on pass."},
  ], "build": {"title": "Deploy a model as an API with monitoring",
    "A": "Serve your model via FastAPI/BentoML with schemas, containerized.",
    "B": "Add monitoring: drift detection, latency/throughput metrics and alerting, plus a shadow-mode path.",
    "C": "Wire CI/CD with a model-validation gate and deploy — a flagship MLOps portfolio project."},
  "cons": {"title": "Keeping models alive",
    "text": "Serving, batch vs online, monitoring, safe rollout and CI/CD are what keep a model useful after launch. Re-check that your drift monitor fires.",
    "mastery": "How would you detect that a live model has silently degraded, and what's your safe path to ship a replacement?"}},

 {"title": "ML System Design & Scale", "days": [
   {"t": "Designing ML systems (a framework)",
    "concept": "Framing an ML problem: objective, constraints, data, baseline, metrics, and the design-doc discipline.",
    "watch": "Chip Huyen — Designing ML Systems; search 'ML system design interview'.",
    "code": "Write a full ML design doc for a realistic problem (fraud/recsys/churn)."},
   {"t": "Scaling training: distributed & data-parallel",
    "concept": "Data vs model parallelism, and scaling training across GPUs/nodes when data or models don't fit.",
    "watch": "Search 'distributed training data parallel explained'.",
    "code": "Run a data-parallel training job (PyTorch DDP or Spark ML) and measure the scaling."},
   {"t": "Feature/label pipelines at scale",
    "concept": "Building training data at scale, point-in-time correctness, and label generation / weak supervision.",
    "watch": "Chip Huyen — training data; search 'weak supervision Snorkel'.",
    "code": "Build a point-in-time-correct training set from event logs (no future leakage)."},
   {"t": "Cost & latency tradeoffs",
    "concept": "Model size vs accuracy vs latency vs cost, caching predictions, and choosing the cheapest thing that works.",
    "watch": "Search 'ML inference cost optimization'.",
    "code": "Profile inference cost/latency and cut it (quantization/caching/simpler model) within an accuracy budget."},
   {"t": "Case studies: fraud, recsys",
    "concept": "Reading real ML system designs (feature pipelines, serving, feedback loops) to build pattern intuition.",
    "watch": "Search 'Uber Michelangelo' and 'Netflix recommendations system design'.",
    "code": "Reverse-engineer a public ML system design into a diagram + tradeoff notes."},
  ], "build": {"title": "ML system-design doc + a prototype",
    "A": "Choose a realistic problem and write a full system-design doc (objective, data, features, model, serving, monitoring, tradeoffs).",
    "B": "Prototype the core: a training pipeline + a served model + basic monitoring.",
    "C": "Present it as a design doc + working prototype — exactly the artifact senior ML interviews want."},
  "cons": {"title": "Thinking at system scale",
    "text": "Problem framing, distributed training, scaled data pipelines, cost/latency and case studies build ML system-design judgment. Re-read your design doc critically.",
    "mastery": "Walk an interviewer through your ML system design: objective, data, baseline, serving, monitoring, and the top three tradeoffs."}},

 {"title": "ML Capstone", "days": [
   {"t": "Problem framing & data",
    "concept": "Pick a real, meaningful ML problem; define the objective and metric and assemble/clean the data.",
    "watch": "Revisit Chip Huyen's framing + your EDA notes.",
    "code": "Write the capstone problem statement and success metric, and load/clean the data."},
   {"t": "Feature & label pipelines",
    "concept": "Build leak-free feature and label pipelines with point-in-time correctness.",
    "watch": "Reuse your feature-engineering + feature-store work.",
    "code": "Build the feature/label pipeline with tests and no future leakage."},
   {"t": "Modeling & evaluation",
    "concept": "Baseline -> improved model with honest cross-validation, the right metric, and error analysis.",
    "watch": "Reuse your evaluation toolkit.",
    "code": "Train baseline + improved models, evaluate rigorously, and do error analysis."},
   {"t": "Serving",
    "concept": "Serve the model as an API with schemas, containerized, ready for monitoring.",
    "watch": "Reuse your serving + MLOps work.",
    "code": "Serve the model (FastAPI/BentoML), containerized, with a typed API."},
   {"t": "Monitoring & writeup",
    "concept": "Add drift/performance monitoring and write the portfolio narrative + model card.",
    "watch": "Reuse your monitoring + interpretability work.",
    "code": "Add monitoring, write the README/model card, and record a demo."},
  ], "build": {"title": "ML-product capstone — build & ship",
    "A": "Assemble the full ML product: data -> features -> model -> API, reproducible and tracked (MLflow/DVC).",
    "B": "Add monitoring (drift, performance), interpretability (SHAP) and CI/CD.",
    "C": "Ship it: repo, model card, demo — your flagship Data Science / ML portfolio project."},
  "cons": {"title": "Phase 2 review",
    "text": "You've gone from math to a shipped, monitored ML product. Step back: which parts felt weakest, and how will the AI phase build on this foundation?",
    "mastery": "Give the 3-minute tour of your ML product: the problem, the data/features, why this model, and how you'd know if it broke in production. Spaced recall (no notes): explain three Data-Engineering ideas you'd started to forget — an LSM-tree, a Kafka consumer group, and a star schema."}},

 # ============================ PHASE 3 — DEEP LEARNING & AI ==================
 {"title": "Neural Networks — Foundations", "days": [
   {"t": "Perceptron & MLPs",
    "concept": "A neuron is a weighted sum + a nonlinearity; stack them into layers (an MLP) and you can approximate any function (universal approximation).",
    "watch": "3Blue1Brown — Neural Networks ch.1; Andrej Karpathy — 'The spelled-out intro to neural networks' (micrograd).",
    "code": "Implement a single neuron and a 2-layer MLP forward pass in NumPy."},
   {"t": "Backpropagation from scratch",
    "concept": "Backprop is reverse-mode autodiff: compute local gradients over a computation graph and chain them back to update weights.",
    "watch": "Karpathy — micrograd ('building an autograd engine'); 3Blue1Brown — backpropagation.",
    "code": "Build a tiny autograd engine (micrograd-style) and train an MLP on a toy dataset."},
   {"t": "Activations & initialization",
    "concept": "ReLU/GELU/sigmoid/tanh and why they matter (vanishing gradients); why init (Xavier/He) makes training even possible.",
    "watch": "Karpathy — makemore (activations, init, batchnorm); search 'weight initialization deep learning'.",
    "code": "Compare training with different activations + inits and watch gradients vanish or explode."},
   {"t": "Loss functions & optimizers (Adam)",
    "concept": "Cross-entropy vs MSE, and the optimizers (SGD, momentum, RMSProp, Adam) that shape convergence.",
    "watch": "Search 'Adam optimizer explained'; Karpathy — makemore.",
    "code": "Train the same net with SGD vs Adam and compare convergence."},
   {"t": "Regularization: dropout & batchnorm",
    "concept": "Dropout, batch/layer norm, weight decay and early stopping — the tricks that make deep nets generalize and train stably.",
    "watch": "Karpathy — makemore (batchnorm); search 'dropout batchnorm explained'.",
    "code": "Add dropout + batchnorm to your net and measure the effect on overfitting."},
  ], "build": {"title": "Neural net from scratch (NumPy) then PyTorch",
    "A": "Build a full MLP from scratch in NumPy: forward, backprop, an optimizer, a training loop — train it on MNIST-ish data.",
    "B": "Reimplement it in PyTorch and match the results; note what the framework automates (autograd, optimizers).",
    "C": "Write up 'what a neural net really computes' with your from-scratch code — it proves deep understanding."},
  "cons": {"title": "What a network really computes",
    "text": "Neurons/MLPs, backprop, activations/init, losses/optimizers and regularization are the machinery of every deep model. Re-run your micrograd and watch the gradients flow.",
    "mastery": "Explain backprop as reverse-mode autodiff, and why initialization and normalization matter for training."}},

 {"title": "PyTorch & Training", "days": [
   {"t": "PyTorch: tensors & autograd",
    "concept": "Tensors (GPU arrays), autograd (the dynamic computation graph), and requires_grad / backward().",
    "watch": "PyTorch official 'Learn the Basics' tutorial.",
    "code": "Recreate your from-scratch net using torch tensors + autograd; verify gradients with .backward()."},
   {"t": "nn.Module & training loops",
    "concept": "Defining models with nn.Module, the canonical training loop (forward -> loss -> backward -> step), and datasets/dataloaders.",
    "watch": "PyTorch tutorials; fast.ai — Practical Deep Learning lesson 1.",
    "code": "Write a clean, reusable training loop with nn.Module, an optimizer and a DataLoader."},
   {"t": "Data loading & augmentation",
    "concept": "Dataset/DataLoader, batching, and augmentation to fight overfitting and expand data.",
    "watch": "PyTorch — data loading tutorial; search 'data augmentation'.",
    "code": "Build a Dataset + DataLoader with augmentation and visualize the augmented batches."},
   {"t": "Training tricks: schedules, mixed precision",
    "concept": "LR schedules (warmup, cosine), mixed precision (amp) for speed/memory, gradient clipping and checkpointing.",
    "watch": "Search 'PyTorch mixed precision amp' and 'learning rate schedule warmup cosine'.",
    "code": "Add a cosine LR schedule + mixed precision to your training loop and measure the speedup."},
   {"t": "Debugging deep nets",
    "concept": "Diagnosing training: loss not dropping, overfitting, NaNs — and Karpathy's recipe for training neural nets.",
    "watch": "Karpathy — 'A Recipe for Training Neural Networks' (blog); search 'debugging neural networks'.",
    "code": "Deliberately break a training run (bad LR, no shuffle) and diagnose each issue from the loss curves."},
  ], "build": {"title": "Reproduce ResNet / ViT from the paper (+ ablations)",
    "A": "Implement ResNet (or a small Vision Transformer) from the paper in PyTorch and train it to a target accuracy on a real dataset (CIFAR-10 or your own).",
    "B": "Ablate the paper's key idea — residual connections for ResNet, or patch size/depth for ViT — and measure exactly what it contributes.",
    "C": "Write a reproduction report with training curves and an honest gap-to-paper analysis — research rigor on a recruiter-friendly deep-learning topic."},
  "cons": {"title": "Training that converges",
    "text": "Tensors/autograd, nn.Module loops, data loading, training tricks and debugging are the day-to-day of deep learning. Re-read Karpathy's recipe.",
    "mastery": "Walk through one full training step in PyTorch, and your checklist when the loss won't drop."}},

 {"title": "Computer Vision & CNNs", "days": [
   {"t": "Convolutions & CNN architectures",
    "concept": "Convolutions (local, shared weights), pooling, and how CNNs build hierarchical features — the conv/pool/FC pattern.",
    "watch": "Stanford CS231n — CNNs; StatQuest — convolutional neural networks.",
    "code": "Implement a conv layer forward pass by hand, then build a small CNN in PyTorch."},
   {"t": "Classic architectures (ResNet, etc.)",
    "concept": "LeNet -> AlexNet -> VGG -> ResNet (skip connections that enable very deep nets); what each innovation solved.",
    "watch": "Search 'ResNet explained'; CS231n architectures lecture.",
    "code": "Build a small ResNet with skip connections and train it; compare to a plain deep CNN."},
   {"t": "Transfer learning",
    "concept": "Reusing pretrained backbones (ImageNet) and fine-tuning — strong results with little data.",
    "watch": "fast.ai — transfer learning; the PyTorch transfer-learning tutorial.",
    "code": "Fine-tune a pretrained ResNet on a small custom dataset and beat training-from-scratch."},
   {"t": "Detection & segmentation intro",
    "concept": "Beyond classification: object detection (YOLO/Faster R-CNN) and segmentation (U-Net/Mask R-CNN) at a high level.",
    "watch": "Search 'object detection YOLO explained' and 'image segmentation U-Net'.",
    "code": "Run a pretrained detector (YOLO) on your images and interpret the boxes/scores."},
   {"t": "Vision in practice",
    "concept": "Data quality, class imbalance, evaluation (mAP/IoU), and deploying a vision model.",
    "watch": "Search 'computer vision production pitfalls'.",
    "code": "Evaluate your fine-tuned model with a confusion matrix + per-class metrics and find its failure modes."},
  ], "build": {"title": "Fine-tune a CNN for a real vision task",
    "A": "Pick a real vision task/dataset; set up data, augmentation and a pretrained backbone.",
    "B": "Fine-tune it, tune the head/LR, and evaluate rigorously (per-class, failure cases).",
    "C": "Serve it behind a small API with a demo UI and write up the transfer-learning approach — a portfolio CV project."},
  "cons": {"title": "Seeing with networks",
    "text": "Convolutions, classic architectures, transfer learning, detection/segmentation and practical evaluation are the CV toolkit. Re-read your model's failure modes.",
    "mastery": "Why do skip connections let nets go deeper, and when does transfer learning beat training from scratch?"}},

 {"title": "Sequence Models & NLP", "days": [
   {"t": "RNNs, LSTMs, GRUs",
    "concept": "Modeling sequences with recurrence, the vanishing-gradient problem, and how LSTM/GRU gates fix long-range memory.",
    "watch": "Karpathy — 'The Unreasonable Effectiveness of RNNs' (blog); StatQuest — RNNs/LSTMs.",
    "code": "Build a character-level RNN/LSTM in PyTorch and generate text."},
   {"t": "Word embeddings (word2vec, GloVe)",
    "concept": "Dense word vectors capturing meaning (king - man + woman = queen); skip-gram/CBOW and how embeddings are learned.",
    "watch": "Jay Alammar — 'The Illustrated Word2vec'.",
    "code": "Train word2vec on a corpus and explore analogies + nearest neighbors."},
   {"t": "Tokenization & text preprocessing",
    "concept": "From text to tokens: word/subword (BPE) tokenization, normalization, and why tokenization shapes everything downstream.",
    "watch": "Karpathy — 'Let's build the GPT Tokenizer'; HuggingFace — tokenizers.",
    "code": "Compare word-level vs BPE tokenization on a corpus; inspect the vocab and OOV behavior."},
   {"t": "seq2seq & attention",
    "concept": "Encoder-decoder for translation/summarization, and attention as the fix for the fixed-context bottleneck — the bridge to Transformers.",
    "watch": "Jay Alammar — 'Visualizing seq2seq with attention'; Stanford CS224N — attention.",
    "code": "Build a small seq2seq model with attention for a toy translation/copy task."},
   {"t": "Classic NLP tasks",
    "concept": "Text classification, NER, sentiment and NLP evaluation — the tasks LLMs now subsume but you should understand.",
    "watch": "HuggingFace NLP course (early chapters); CS224N.",
    "code": "Build a text classifier (embeddings + a small net) and evaluate it properly."},
  ], "build": {"title": "Text classifier / seq2seq model",
    "A": "Build a solid text model: a classifier (embeddings -> net) or a seq2seq-with-attention for a small task.",
    "B": "Add proper tokenization, training, and NLP-appropriate evaluation.",
    "C": "Write up how attention solved the seq2seq bottleneck — the perfect setup for next week's Transformers."},
  "cons": {"title": "Modeling sequences",
    "text": "RNNs/LSTMs, embeddings, tokenization, seq2seq + attention and classic NLP are the pre-Transformer world — and attention is the thread into it. Re-read the Illustrated Word2vec.",
    "mastery": "Why do RNNs struggle with long sequences, and what problem does attention solve in seq2seq?"}},

 {"title": "Transformers I — Architecture", "days": [
   {"t": "The attention mechanism, deep",
    "concept": "Query/key/value attention: every token attends to every other, weighted by relevance — the core operation of modern AI.",
    "watch": "3Blue1Brown — attention; Jay Alammar — 'The Illustrated Transformer'.",
    "code": "Implement scaled dot-product attention from scratch in PyTorch and verify shapes/softmax."},
   {"t": "The Transformer: encoder / decoder",
    "concept": "The full architecture: multi-head attention, feed-forward blocks, residual connections + layernorm, encoder vs decoder stacks.",
    "watch": "Jay Alammar — Illustrated Transformer; skim 'Attention Is All You Need'.",
    "code": "Assemble a single Transformer block (attention + FFN + residual + norm) in PyTorch."},
   {"t": "Self-attention, multi-head, positional encoding",
    "concept": "Multiple heads capture different relations; positional encodings inject order into an otherwise order-blind mechanism.",
    "watch": "Karpathy — 'Let's build GPT' (self-attention section).",
    "code": "Add multi-head attention + positional encodings to your block and visualize an attention matrix."},
   {"t": "BERT vs GPT families",
    "concept": "Encoder-only (BERT, bidirectional, understanding) vs decoder-only (GPT, autoregressive, generation) vs encoder-decoder (T5) — and what each is for.",
    "watch": "Jay Alammar — 'The Illustrated GPT-2' and 'Illustrated BERT'.",
    "code": "Load BERT and GPT-2 from HuggingFace and compare what each does on the same text."},
   {"t": "Building a transformer block",
    "concept": "Putting it together into a working decoder-only LM: embeddings, N blocks, a head, and the training objective.",
    "watch": "Karpathy — 'Let's build GPT: from scratch'.",
    "code": "Wire embeddings + stacked blocks + an output head into a tiny GPT and do a forward pass."},
  ], "build": {"title": "Transformer from scratch (nanoGPT-style)",
    "A": "Build a decoder-only Transformer (GPT) from scratch in PyTorch following Karpathy's 'Let's build GPT': attention, blocks, positional encodings.",
    "B": "Train it on a text corpus (tiny-shakespeare or your own) and generate samples.",
    "C": "Annotate every component with what it does + why; publish a 'built a GPT from scratch' repo — a flagship AI project."},
  "cons": {"title": "Attention is the engine",
    "text": "Attention, the full Transformer, multi-head + positions, the BERT/GPT split and a working block are the foundation of everything modern in AI. Re-watch Karpathy's GPT build.",
    "mastery": "Explain self-attention with Q/K/V in one paragraph, and why positional encodings are necessary."}},

 {"title": "Transformers II — Pretraining", "days": [
   {"t": "Pretraining objectives (MLM, CLM)",
    "concept": "Masked language modeling (BERT) vs causal/next-token (GPT); how self-supervised pretraining learns from raw text at scale.",
    "watch": "Jay Alammar — Illustrated BERT; search 'MLM vs CLM pretraining'.",
    "code": "Implement next-token (causal) training for your nanoGPT and track the loss."},
   {"t": "Tokenizers: BPE & SentencePiece",
    "concept": "Byte-pair encoding and SentencePiece: how subword vocabularies are built, and their effect on cost and multilinguality.",
    "watch": "Karpathy — 'Let's build the GPT Tokenizer'; HuggingFace tokenizers.",
    "code": "Train a BPE tokenizer on your corpus and plug it into your GPT."},
   {"t": "Scaling laws & model families",
    "concept": "How loss scales with params/data/compute (Chinchilla), and the landscape of open vs closed model families.",
    "watch": "Search 'scaling laws Chinchilla explained'.",
    "code": "Train your small GPT at 2-3 sizes and plot loss vs params/compute — see scaling firsthand."},
   {"t": "The HuggingFace ecosystem",
    "concept": "transformers, datasets, tokenizers and the Hub; loading, running and sharing pretrained models the standard way.",
    "watch": "HuggingFace — NLP course (using Transformers).",
    "code": "Load a pretrained model + tokenizer from the Hub, run inference, and push a model/dataset."},
   {"t": "Fine-tuning basics",
    "concept": "Full fine-tuning a pretrained model on a downstream task, and the train/eval loop with the HuggingFace Trainer.",
    "watch": "HuggingFace — fine-tuning a pretrained model.",
    "code": "Fine-tune a small pretrained model (e.g. DistilBERT) on a classification task with Trainer."},
  ], "build": {"title": "Train a small GPT on a custom corpus",
    "A": "Take your nanoGPT, add a trained BPE tokenizer and causal pretraining, and train on a corpus you care about.",
    "B": "Scale it up a little, add eval (perplexity) and sampling controls (temperature/top-k).",
    "C": "Write up the pretraining run + scaling observations — deep, credible AI portfolio content."},
  "cons": {"title": "How LLMs are made",
    "text": "Pretraining objectives, tokenizers, scaling laws, the HuggingFace ecosystem and fine-tuning basics are how raw text becomes a language model. Re-read your loss/perplexity curves.",
    "mastery": "Contrast MLM and causal pretraining, and what scaling laws say about spending compute on params vs data."}},

 {"title": "LLMs I — Understanding & Using", "days": [
   {"t": "Inference, sampling & decoding",
    "concept": "How an LLM generates: logits -> sampling (greedy, temperature, top-k, top-p), and why decoding settings change outputs.",
    "watch": "Search 'LLM sampling temperature top-p explained'.",
    "code": "Take a HuggingFace LLM and generate with different decoding settings; observe the effect."},
   {"t": "Prompting techniques",
    "concept": "Zero/few-shot, chain-of-thought, structured prompts, and system/user roles — reliable behavior from a fixed model.",
    "watch": "Search 'prompt engineering guide'; Anthropic prompt-engineering docs.",
    "code": "Take a task and improve results measurably by iterating prompts (zero-shot -> few-shot -> CoT)."},
   {"t": "Context, tokens & cost",
    "concept": "Context windows, tokenization cost, managing long inputs (truncation, chunking), and latency/price tradeoffs.",
    "watch": "Search 'LLM context window tokens explained'.",
    "code": "Measure tokens + cost for prompts of different sizes and build a token budget for an app."},
   {"t": "LLM APIs & the ecosystem",
    "concept": "Calling LLM APIs (messages, system prompts, streaming, tools), and the open-vs-hosted landscape.",
    "watch": "Read a major provider's messages/API docs (e.g. Anthropic/OpenAI).",
    "code": "Build a small app that calls an LLM API with a system prompt and streams the response."},
   {"t": "LLM evaluation",
    "concept": "Why eval is hard, and approaches: golden sets, rubric/LLM-as-judge, and task-specific metrics — eval-driven development.",
    "watch": "Chip Huyen — evaluating LLM apps; search 'LLM evaluation methods'.",
    "code": "Build a small eval harness (golden examples + an LLM-as-judge rubric) for a task."},
  ], "build": {"title": "LLM app with strong prompting + an eval harness",
    "A": "Build a real LLM feature (summarizer, extractor, assistant) via an API with a solid system prompt.",
    "B": "Add structured output, error handling, streaming and a token/cost budget.",
    "C": "Build an eval harness (golden set + LLM-judge) and iterate prompts against it — eval-driven, portfolio-grade."},
  "cons": {"title": "Using LLMs well",
    "text": "Decoding, prompting, context/cost, APIs and evaluation are the practical foundation of building with LLMs. Re-read your eval results.",
    "mastery": "How does temperature/top-p change generation, and why is a golden-set + judge eval better than eyeballing outputs?"}},

 {"title": "LLMs II — Embeddings & RAG", "days": [
   {"t": "Embeddings, deep",
    "concept": "Text embeddings map meaning to vectors; similarity search, and choosing/measuring an embedding model.",
    "watch": "Jay Alammar — illustrated embeddings; search 'text embeddings explained'.",
    "code": "Embed a corpus with an embedding model and build semantic search over it (cosine similarity)."},
   {"t": "Vector databases (pgvector, etc.)",
    "concept": "Storing + searching embeddings at scale: ANN indexes (HNSW) and pgvector/FAISS/other stores.",
    "watch": "Search 'vector database explained'; pgvector docs.",
    "code": "Load embeddings into pgvector (or FAISS) and run fast approximate nearest-neighbor search."},
   {"t": "RAG architecture",
    "concept": "Retrieval-augmented generation: retrieve relevant chunks, put them in the prompt, generate a grounded answer — the dominant enterprise LLM pattern.",
    "watch": "Search 'RAG explained'; Chip Huyen / Anthropic on RAG.",
    "code": "Build a minimal RAG: chunk -> embed -> retrieve -> prompt -> answer over your own docs."},
   {"t": "Chunking, retrieval & reranking",
    "concept": "Chunking strategies, hybrid search (keyword + vector), reranking and query rewriting — where RAG quality is won or lost.",
    "watch": "Search 'RAG chunking strategies reranking'.",
    "code": "Improve your RAG's retrieval with better chunking + a reranker; measure the answer-quality lift."},
   {"t": "RAG evaluation",
    "concept": "Evaluating RAG: retrieval metrics (recall/precision@k) + answer faithfulness/groundedness (RAGAS-style).",
    "watch": "Search 'RAG evaluation RAGAS'.",
    "code": "Build a RAG eval set and score retrieval + faithfulness; find and fix the weakest link."},
  ], "build": {"title": "Production RAG over your own corpus",
    "A": "Build a real RAG system over a corpus you care about (your notes, docs, a codebase): ingest -> chunk -> embed -> vector store.",
    "B": "Add hybrid retrieval + reranking, query rewriting, citations, and a chat UI/API.",
    "C": "Add a RAG eval harness (retrieval + faithfulness) and iterate; ship it — a flagship AI portfolio project."},
  "cons": {"title": "Grounding LLMs in data",
    "text": "Embeddings, vector DBs, the RAG loop, chunking/reranking and RAG eval are the most in-demand LLM skill set right now. Re-read your RAG eval scores.",
    "mastery": "Walk through a RAG query end to end, and name the two places retrieval quality most often breaks."}},

 {"title": "LLMs III — Fine-tuning", "days": [
   {"t": "Full fine-tuning, LoRA, QLoRA",
    "concept": "When to fine-tune, full vs parameter-efficient (LoRA freezes the model and trains small adapters; QLoRA adds quantization).",
    "watch": "Sebastian Raschka — LoRA/fine-tuning; search 'LoRA QLoRA explained'.",
    "code": "Fine-tune a small open model with LoRA (PEFT) on a task dataset."},
   {"t": "PEFT & adapters",
    "concept": "Parameter-efficient fine-tuning (LoRA, adapters, prefix/prompt tuning) — big gains, tiny trainable footprints.",
    "watch": "HuggingFace PEFT docs; Sebastian Raschka.",
    "code": "Compare LoRA rank / target-module choices and their effect on quality + trainable params."},
   {"t": "Instruction tuning; RLHF/DPO intuition",
    "concept": "Turning a base model into an assistant: instruction tuning, and preference alignment (RLHF, DPO) at an intuition level.",
    "watch": "Search 'RLHF explained' and 'DPO direct preference optimization'.",
    "code": "Instruction-tune a small model on an instruction dataset and compare to the base model."},
   {"t": "Quantization & distillation",
    "concept": "Shrinking models for cheaper inference: quantization (int8/int4, GGUF) and distillation (a small model learns from a big one).",
    "watch": "Search 'LLM quantization explained GGUF' and 'knowledge distillation'.",
    "code": "Quantize your fine-tuned model to int4 and measure the size/latency vs quality tradeoff."},
   {"t": "When to tune vs RAG vs prompt",
    "concept": "The decision framework: prompting for behavior, RAG for knowledge, fine-tuning for style/format/skill — and their costs.",
    "watch": "Chip Huyen; search 'fine-tuning vs RAG vs prompting'.",
    "code": "Take one task and solve it three ways (prompt, RAG, fine-tune); compare quality, cost and effort."},
  ], "build": {"title": "Fine-tune an open LLM with LoRA",
    "A": "Pick a task where fine-tuning helps (style, format, a domain skill) and prepare a clean instruction dataset.",
    "B": "Fine-tune an open model with LoRA/QLoRA; evaluate against the base model + a prompted baseline.",
    "C": "Quantize + serve it, and write up the tune-vs-RAG-vs-prompt decision — a strong applied-AI portfolio piece."},
  "cons": {"title": "Adapting models to you",
    "text": "Full vs PEFT fine-tuning, instruction tuning/alignment, quantization/distillation and the tune-vs-RAG-vs-prompt decision are how you shape models. Re-read your three-way comparison.",
    "mastery": "How does LoRA fine-tune a model without touching most of its weights, and when do you fine-tune instead of RAG?"}},

 {"title": "AI Agents I", "days": [
   {"t": "Agent concepts: ReAct & planning",
    "concept": "Agents = an LLM + tools + a loop; ReAct (reason + act), planning, and the reason-act-observe cycle.",
    "watch": "Search 'ReAct agents explained'; Anthropic — 'Building effective agents'.",
    "code": "Build a minimal ReAct loop by hand (no framework): reason -> call a tool -> observe -> repeat."},
   {"t": "Tool use & function calling",
    "concept": "Giving models tools via function/tool calling: schemas, the call loop, and parsing/validating tool outputs.",
    "watch": "A provider's tool-use/function-calling docs (Anthropic/OpenAI).",
    "code": "Give your agent 2-3 real tools (search, calculator, an API) via function calling and let it use them."},
   {"t": "Frameworks vs framework-light",
    "concept": "LangChain/LlamaIndex/LangGraph vs writing it yourself; why framework-light is often better for control and debugging.",
    "watch": "Anthropic — 'Building effective agents' (the framework caution); search 'do you need langchain'.",
    "code": "Rebuild your agent both with a framework and framework-light; compare control and debuggability."},
   {"t": "Memory & state",
    "concept": "Short-term (context) vs long-term memory (a vector store), conversation state and scratchpads.",
    "watch": "Search 'agent memory patterns'.",
    "code": "Add long-term memory (a vector store the agent reads/writes) so it recalls past interactions."},
   {"t": "Multi-agent systems",
    "concept": "When multiple specialized agents beat one (orchestrator/worker, debate, review), and the coordination cost.",
    "watch": "Anthropic; search 'multi-agent systems LLM'.",
    "code": "Build a 2-agent setup (a worker + a reviewer/critic) and show it beats a single agent on a task."},
  ], "build": {"title": "Tool-using agent for a real task (fleet-style)",
    "A": "Build an agent that does a genuinely useful task with tools (like your own bot fleet): define the tools + the loop, framework-light.",
    "B": "Add memory, error handling and guardrails; make it reliable, not a demo.",
    "C": "Deploy it (a scheduled job or a bot) and document the design — a flagship, on-brand AI portfolio project."},
  "cons": {"title": "Agents that act",
    "text": "ReAct + planning, tool use, framework choices, memory and multi-agent patterns are how LLMs do things, not just talk. Re-read Anthropic's 'Building effective agents'.",
    "mastery": "Describe the reason-act-observe loop, and when a multi-agent design actually beats a single well-prompted agent."}},

 {"title": "AI Agents II & MCP", "days": [
   {"t": "MCP (Model Context Protocol), deep",
    "concept": "MCP standardizes how models connect to tools/data (servers expose tools/resources/prompts) — the 'USB-C for AI tools'.",
    "watch": "modelcontextprotocol.io docs; search 'Model Context Protocol explained'.",
    "code": "Read the MCP spec and run an existing MCP server with a client."},
   {"t": "Building MCP servers",
    "concept": "Writing an MCP server that exposes tools + resources so any MCP client (Claude, IDEs) can use your data/actions.",
    "watch": "MCP docs — build a server (Python/TS SDK).",
    "code": "Build an MCP server exposing 2-3 tools over your data (e.g. your data platform) and connect a client."},
   {"t": "Agent-orchestration patterns",
    "concept": "Orchestrator/worker, routing, chaining and evaluator-optimizer loops — deterministic control around non-deterministic models.",
    "watch": "Anthropic — 'Building effective agents' (patterns).",
    "code": "Implement an orchestrator that routes a task to the right tool/sub-agent deterministically."},
   {"t": "Evaluation & guardrails",
    "concept": "Evaluating agents (task success, trajectory), and guardrails: input/output validation, allow-lists and human-in-the-loop.",
    "watch": "Search 'evaluating AI agents'; guardrails frameworks.",
    "code": "Add guardrails (a tool allow-list, output validation) + a task-success eval to your agent."},
   {"t": "Cost, latency & reliability",
    "concept": "Agents are expensive and flaky by default: caching, step limits, retries, and cost/latency budgets.",
    "watch": "Search 'production AI agents reliability cost'.",
    "code": "Add step limits, caching and retries; measure and cut your agent's cost per task."},
  ], "build": {"title": "MCP server + agent over your data platform",
    "A": "Build an MCP server that exposes your Data-Engineering platform (query data, check pipeline status) as tools.",
    "B": "Build an agent that uses it to answer real questions / do real tasks, with orchestration + guardrails.",
    "C": "Add evaluation + cost controls and document it — this ties your DE, AI and infra work into one showcase."},
  "cons": {"title": "Wiring agents to tools",
    "text": "MCP, building servers, orchestration patterns, evaluation/guardrails and cost/reliability are how agents become dependable products. Re-read your guardrails.",
    "mastery": "What problem does MCP standardize, and what are the three things that keep a production agent reliable and affordable?"}},

 {"title": "LLMOps & Production AI", "days": [
   {"t": "Serving LLMs (vLLM, TGI)",
    "concept": "Efficient LLM serving with vLLM/TGI: continuous batching, paged attention, and why naive serving is slow and expensive.",
    "watch": "Search 'vLLM paged attention'; the vLLM docs.",
    "code": "Serve an open model with vLLM and benchmark throughput vs a naive HuggingFace pipeline."},
   {"t": "Inference optimization: KV cache, batching",
    "concept": "The KV cache, continuous batching, quantization and speculative decoding — the levers of fast, cheap inference.",
    "watch": "Search 'KV cache explained' and 'continuous batching LLM'.",
    "code": "Measure the effect of the KV cache + batching on latency/throughput for your served model."},
   {"t": "Observability: tracing & evals",
    "concept": "Tracing LLM apps (spans, tokens, cost), online evals, and catching quality regressions in production.",
    "watch": "Search 'LLM observability tracing' (LangSmith/Langfuse concepts).",
    "code": "Add tracing (tokens, latency, cost per request) + an online eval to an LLM app."},
   {"t": "Guardrails & prompt-injection defense",
    "concept": "Prompt injection, jailbreaks, and defenses: input/output filtering, allow-lists, and untrusted-content isolation.",
    "watch": "Search 'prompt injection explained'; the OWASP LLM Top 10.",
    "code": "Attack your own LLM app with prompt injection, then add defenses and re-test."},
   {"t": "Cost at scale",
    "concept": "Token economics, caching, model routing (small model first) and batching — running AI affordably.",
    "watch": "Search 'LLM cost optimization strategies'.",
    "code": "Add a cache + model-routing (cheap model first, escalate) to an app and measure the cost drop."},
  ], "build": {"title": "Deploy an LLM app with evals + guardrails",
    "A": "Take your RAG/agent app and deploy it properly (vLLM or an API) with schemas + streaming.",
    "B": "Add observability (tracing, cost), online evals, and guardrails (injection defense, output validation).",
    "C": "Load-test it, add cost controls, and document the LLMOps setup — a portfolio production-AI piece."},
  "cons": {"title": "Operating AI in production",
    "text": "Serving, inference optimization, observability, guardrails and cost control separate an AI demo from an AI product. Re-run your injection tests.",
    "mastery": "What does continuous batching + a KV cache do for LLM serving, and how do you defend against prompt injection?"}},

 {"title": "Advanced AI", "days": [
   {"t": "Multimodal models",
    "concept": "Vision-language models (CLIP, LLaVA, GPT-4V-style): how images + text share an embedding space and enable new tasks.",
    "watch": "Search 'CLIP explained' and 'multimodal LLMs'.",
    "code": "Use CLIP for zero-shot image classification / image-text search."},
   {"t": "Diffusion models (generation)",
    "concept": "How diffusion generates images by iterative denoising; latent diffusion (Stable Diffusion) and conditioning.",
    "watch": "Search 'how diffusion models work'; Jay Alammar — illustrated stable diffusion.",
    "code": "Run Stable Diffusion, then read a from-scratch diffusion tutorial to understand the denoising loop."},
   {"t": "Structured generation & function calling",
    "concept": "Constraining LLM output to schemas/JSON/grammars for reliable downstream use.",
    "watch": "Search 'structured output LLM json schema' (outlines/instructor).",
    "code": "Force reliable JSON output from an LLM with a schema and validate it."},
   {"t": "Knowledge graphs + LLMs",
    "concept": "Combining structured knowledge (graphs) with LLMs (GraphRAG) for grounded, multi-hop reasoning.",
    "watch": "Search 'GraphRAG explained'.",
    "code": "Build a tiny knowledge graph and use it to ground/augment LLM answers (GraphRAG-lite)."},
   {"t": "Frontier reading",
    "concept": "Staying current: reading papers (arXiv, Papers with Code), model releases, and separating signal from hype.",
    "watch": "Search 'how to read ML papers'.",
    "code": "Read one recent influential paper and write a one-page summary + how you'd use it."},
  ], "build": {"title": "Reproduce CLIP-style contrastive training",
    "A": "Implement contrastive image-text pretraining (CLIP-style) at small scale: dual encoders, the InfoNCE loss, and a shared embedding space.",
    "B": "Train on a small image-caption dataset and evaluate zero-shot classification and image-text retrieval — the capabilities CLIP is known for.",
    "C": "Write up the method and your zero-shot results — a multimodal piece with genuine research depth."},
  "cons": {"title": "The expanding frontier",
    "text": "Multimodal, diffusion, structured generation, KG+LLM and paper-reading keep you current as the field moves. Re-read your paper summary.",
    "mastery": "How does CLIP put images and text in one space, and how does diffusion turn noise into an image?"}},

 {"title": "AI Engineering", "days": [
   {"t": "AI product design & UX",
    "concept": "Designing AI products around model strengths/weaknesses: managing uncertainty, latency, and setting user expectations.",
    "watch": "Chip Huyen — AI Engineering; search 'designing AI product UX'.",
    "code": "Redesign an existing feature as an AI feature, explicitly handling errors/uncertainty in the UX."},
   {"t": "Evaluation-driven development",
    "concept": "Building AI features test-first: define evals, iterate against them, and treat evals as the spec.",
    "watch": "Chip Huyen — evaluation; search 'eval driven development LLM'.",
    "code": "For a feature, write the eval set first, then build until it passes — eval-driven."},
   {"t": "Data flywheels",
    "concept": "Using production data (feedback, corrections) to continuously improve the system — the moat of AI products.",
    "watch": "Search 'data flywheel AI product'.",
    "code": "Add a feedback-capture loop to an app that logs corrections for future improvement."},
   {"t": "Hybrid systems (classical + LLM)",
    "concept": "Combining deterministic code/classical ML with LLMs — using each where it's strong (cheap/reliable vs flexible).",
    "watch": "Search 'hybrid AI systems classical ML LLM'.",
    "code": "Build a hybrid: a cheap classifier/rules router that only calls the LLM when needed."},
   {"t": "Case studies",
    "concept": "Studying real AI products (how they use retrieval, evals, guardrails, feedback) to build product judgment.",
    "watch": "Read engineering blogs (Anthropic and others) on shipped AI features.",
    "code": "Reverse-engineer a shipped AI product into an architecture + tradeoffs writeup."},
  ], "build": {"title": "Ship a polished AI product feature",
    "A": "Build a genuinely useful, polished AI feature end to end (not a demo): clear UX, error handling, evals.",
    "B": "Make it a hybrid where sensible (classical + LLM) and add a feedback/data-flywheel loop.",
    "C": "Deploy it with observability + guardrails and write the product case study — portfolio-grade AI engineering."},
  "cons": {"title": "Building AI products",
    "text": "Product design, eval-driven development, data flywheels, hybrid systems and case studies are what AI engineering actually is. Re-read your case study.",
    "mastery": "Why is eval-driven development the right way to build with LLMs, and where should classical code do the work instead of the model?"}},

 {"title": "AI Capstone", "days": [
   {"t": "Architecture & data",
    "concept": "Scope an ambitious AI system; design the architecture (retrieval/agent/model) and assemble the data/knowledge.",
    "watch": "Reuse Chip Huyen + Anthropic agent patterns.",
    "code": "Write the AI-system design doc + architecture diagram and prepare the data/corpus."},
   {"t": "Retrieval / agent layer",
    "concept": "Build the core intelligence: RAG and/or an agent with tools over your data.",
    "watch": "Reuse your RAG + agent work.",
    "code": "Build the retrieval/agent layer with the right tools + memory."},
   {"t": "Model integration",
    "concept": "Wire in the model(s): prompting, structured output, and fine-tuning where it helps.",
    "watch": "Reuse your prompting/fine-tuning work.",
    "code": "Integrate the model with strong prompts/structured output; fine-tune if it earns its keep."},
   {"t": "Evaluation harness & guardrails",
    "concept": "Build the eval harness (task success, faithfulness) and guardrails before you trust it.",
    "watch": "Reuse your eval + guardrails work.",
    "code": "Build the eval harness + guardrails and iterate the system against them."},
   {"t": "Portfolio writeup",
    "concept": "Deploy, add observability/cost controls, and write the case study + demo.",
    "watch": "Reuse your LLMOps work.",
    "code": "Deploy with observability, write the case study, and record a demo."},
  ], "build": {"title": "AI-system capstone — build & ship",
    "A": "Assemble an ambitious AI system (RAG + agent + tools) over real data, framework-light and reliable.",
    "B": "Add evals, guardrails, observability and cost controls — production-grade, not a demo.",
    "C": "Ship it: repo, case study, demo — your flagship AI portfolio project."},
  "cons": {"title": "Phase 3 review",
    "text": "You've gone from a neuron to a shipped, evaluated AI system. Step back: what's genuinely yours now, and how will the Linux/systems phase let you productionize all of it?",
    "mastery": "Give the 3-minute tour of your AI system: the architecture, why RAG/agent/fine-tune, how you evaluate it, and how you keep it safe and affordable. Spaced recall (cold): explain exactly-once streaming (DE), gradient boosting vs a neural net (ML), and where each fits in your AI system."}},

 # ============================ PHASE 4 — LINUX & SYSTEMS (LAST) ==============
 {"title": "Linux Basics I — The Command Line", "days": [
   {"t": "Shell navigation, files, redirection & pipes",
    "concept": "The shell as your primary interface: navigating (cd/ls/pwd), managing files, and composing tools with pipes and redirection — the Unix philosophy.",
    "watch": "MIT Missing Semester — the shell; 'The Linux Command Line' (Shotts) ch.1-6.",
    "code": "Do file-management + pipe tasks entirely from the shell; chain 3+ tools with pipes to answer a question."},
   {"t": "The filesystem hierarchy (FHS)",
    "concept": "Where things live: /etc, /var, /usr, /home, /proc, /dev — the map of a Linux system.",
    "watch": "Search 'Linux filesystem hierarchy explained'; The Linux Command Line ch.3.",
    "code": "Explore each top-level directory and note what it holds; find where a package installed its files."},
   {"t": "Permissions & ownership (chmod, chown)",
    "concept": "User/group/other, read/write/execute, octal vs symbolic, and the setuid/sticky bits — Linux's security foundation.",
    "watch": "Search 'Linux file permissions explained'; Missing Semester.",
    "code": "Set up a shared directory with correct group permissions and fix a 'permission denied' from first principles."},
   {"t": "Users, groups & sudo",
    "concept": "Multi-user Linux: /etc/passwd + /etc/group, adding users, group membership, and sudo/root privilege.",
    "watch": "Search 'Linux users and groups tutorial'.",
    "code": "Create a user + group, grant limited sudo, and verify what they can and can't do."},
   {"t": "Package management & man pages",
    "concept": "Installing/updating software (apt/dnf), repositories, and reading man/--help to learn any command.",
    "watch": "Search 'apt package management' and 'how to read man pages'.",
    "code": "Install, query and remove packages; use man + apropos to learn 3 new commands."},
  ], "build": {"title": "Syscall tracer / mini-strace (from scratch)",
    "A": "Use ptrace to build a tiny strace: fork/exec a target process, intercept every system call, and decode the syscall number and its arguments.",
    "B": "Pretty-print arguments (paths, flags, buffers), follow forks, and add timing plus a per-syscall summary like strace -c.",
    "C": "Test it on real programs and write up how syscall interception works — a genuine operating-systems piece, not a shell script."},
  "cons": {"title": "Living on the command line",
    "text": "Navigation/pipes, the FHS, permissions, users/groups and package management are the daily fluency the rest of the phase assumes. Redo a task GUI-free.",
    "mastery": "Explain a permission string like -rwxr-xr-- fully, and what pipes + redirection let you compose."}},

 {"title": "Linux Basics II — Text, Processes & Jobs", "days": [
   {"t": "Text tools: grep, sed, awk, cut, sort",
    "concept": "The text-processing power tools — grep, sed, awk, cut/sort/uniq — that make the shell a serious data tool.",
    "watch": "Search 'awk sed grep tutorial'; Missing Semester — data wrangling.",
    "code": "Parse a log file end to end with grep/sed/awk to extract and summarize a metric."},
   {"t": "Processes & job control",
    "concept": "Viewing processes (ps, top, htop), foreground/background (&, jobs, fg, bg), and signals (kill, nohup).",
    "watch": "Search 'Linux process management ps top'; Julia Evans — processes.",
    "code": "Start/background/foreground jobs, find a runaway process, and kill it with the right signal."},
   {"t": "Environment & shell configuration",
    "concept": "Environment variables, PATH, and shell config (.bashrc/.zshrc, aliases, functions) that shape your session.",
    "watch": "Search 'bash environment variables PATH'; Missing Semester.",
    "code": "Customize your shell: PATH, aliases, a couple of functions, and a decent prompt."},
   {"t": "Redirection, tee, xargs, find",
    "concept": "Advanced composition: stdin/stdout/stderr, tee, xargs for building commands, and find for locating/acting on files.",
    "watch": "Search 'xargs find tutorial'; Julia Evans.",
    "code": "Use find + xargs to batch-process a directory of files and capture output with tee."},
   {"t": "Archives, compression & file transfer",
    "concept": "tar/gzip/zip, rsync for efficient sync/transfer, and scp/sftp over SSH.",
    "watch": "Search 'tar rsync tutorial'.",
    "code": "Archive + compress a directory and rsync it to another host (or VM) efficiently."},
  ], "build": {"title": "A practical shell automation toolkit",
    "A": "Write shell scripts that automate real chores (backups, log parsing, batch renames, a system report).",
    "B": "Make them robust and composable (options, help, error handling) and put them on your PATH.",
    "C": "Package as a small 'toolbox' repo with a README — practical portfolio content you'll actually use."},
  "cons": {"title": "Getting real work done in the shell",
    "text": "Text tools, process/job control, environment config, find/xargs and archiving are the shell as a serious work environment. Redo a chore as a one-liner.",
    "mastery": "How would you find and kill a runaway process, and parse a 1GB log to a summary without opening an editor?"}},

 {"title": "Shell Scripting & Automation", "days": [
   {"t": "Bash: variables, conditionals, loops",
    "concept": "Bash fundamentals: variables, quoting (the classic footgun), conditionals ([[ ]]) and loops.",
    "watch": "Search 'bash scripting tutorial'; The Linux Command Line (Shotts) scripting chapters.",
    "code": "Write a script with arguments, conditionals and a loop that does something useful."},
   {"t": "Functions, arguments & exit codes",
    "concept": "Functions, positional/named arguments, exit codes and $?, and returning data from functions.",
    "watch": "Search 'bash functions exit codes'.",
    "code": "Refactor your script into functions with proper exit codes and a usage/help message."},
   {"t": "Robust scripts: set -euo pipefail, traps",
    "concept": "Writing safe scripts: set -euo pipefail, error traps, cleanup on exit, and shellcheck for linting.",
    "watch": "Search 'bash set -euo pipefail' and 'shellcheck'.",
    "code": "Harden a script with set -euo pipefail + a cleanup trap and fix every shellcheck warning."},
   {"t": "cron & systemd timers",
    "concept": "Scheduling jobs with cron (crontab syntax) and the more powerful systemd timers.",
    "watch": "Search 'crontab tutorial' and 'systemd timers vs cron'.",
    "code": "Schedule your system-report script with both a cron job and a systemd timer."},
   {"t": "jq & structured data in the shell",
    "concept": "Processing JSON/YAML in the shell with jq/yq — essential for APIs and config in modern ops.",
    "watch": "Search 'jq tutorial'.",
    "code": "Call an API with curl and use jq to extract + reshape the JSON into a report."},
  ], "build": {"title": "Mini process supervisor / init (systemd-lite)",
    "A": "Write a process supervisor that spawns services from a config, reaps zombies (PID 1 duties), and restarts them on failure with backoff.",
    "B": "Add dependency ordering, readiness checks, log capture, and graceful shutdown on signals — the core of what systemd does.",
    "C": "Run a small multi-service app under it and write up the process-management model — a from-scratch systems build."},
  "cons": {"title": "Scripting discipline",
    "text": "Bash fundamentals, functions/exit codes, robust-mode + traps, scheduling and jq turn the shell into reliable automation. Re-read your script with shellcheck.",
    "mastery": "Why does set -euo pipefail matter, and when do you reach for a systemd timer over cron?"}},

 {"title": "System Administration & Servers", "days": [
   {"t": "systemd: units, services, journald",
    "concept": "systemd runs modern Linux: unit files, services, targets, enabling/starting, and reading logs with journalctl.",
    "watch": "Search 'systemd tutorial' and 'journalctl explained'.",
    "code": "Write a systemd service unit for your automation script; enable it and read its logs via journalctl."},
   {"t": "Networking basics: ip, ss, DNS, curl",
    "concept": "Inspecting/configuring the network from the shell: ip, ss (sockets), DNS (dig), and curl for HTTP debugging.",
    "watch": "Julia Evans — networking zines; search 'ip ss dig tutorial'.",
    "code": "Diagnose a connectivity problem with ip/ss/dig/curl and find what's listening on a port."},
   {"t": "SSH, keys, tmux & remote work",
    "concept": "SSH (key-based auth, config, tunnels), tmux for persistent sessions, and safe remote workflows.",
    "watch": "Search 'SSH keys tutorial' and 'tmux crash course'.",
    "code": "Set up key-based SSH to a server/VM, an SSH config alias, and a tmux workflow that survives disconnects."},
   {"t": "Disks, mounts, df/du & LVM basics",
    "concept": "Storage admin: partitions, filesystems, mounting, df/du for space, and LVM for flexible volumes.",
    "watch": "Search 'Linux disk management df du mount' and 'LVM basics'.",
    "code": "Add/mount a disk (on a VM), find what's eating space with du, and resize a logical volume."},
   {"t": "Log management & monitoring basics",
    "concept": "System logs, log rotation (logrotate), and basic health monitoring (uptime, load, memory, disk).",
    "watch": "Search 'Linux logging logrotate' and system-monitoring basics.",
    "code": "Set up logrotate for your service's logs and a simple health-check script with alerting."},
  ], "build": {"title": "Provision & harden a real Linux server",
    "A": "Spin up a cloud VM (or a local VM) and provision it: users, SSH keys, firewall, your service via systemd.",
    "B": "Harden it: disable password/root SSH, set up ufw/iptables, automatic updates and fail2ban.",
    "C": "Document the runbook + a from-scratch provision script — a portfolio 'I can run production Linux' piece."},
  "cons": {"title": "Operating a Linux box",
    "text": "systemd, networking tools, SSH/tmux, storage and logging/monitoring are the core of running real servers. Re-check your server's hardening.",
    "mastery": "Walk through provisioning + hardening a fresh server, and how you'd debug 'the service won't start' with systemd + journalctl."}},

 {"title": "Linux Internals I — Processes & Memory", "days": [
   {"t": "Process anatomy: address space, fds (/proc)",
    "concept": "What a process really is: the virtual address space (text/data/heap/stack), the fd table, and the PCB — all visible in /proc.",
    "watch": "OSTEP — processes; Julia Evans — /proc.",
    "code": "Explore /proc/<pid>/ for a running process: maps, fd, status — annotate every region."},
   {"t": "Virtual memory & paging: page tables, TLB, COW",
    "concept": "Every process thinks it owns all memory; page tables translate virtual->physical, the TLB caches translations, and copy-on-write makes fork() cheap.",
    "watch": "OSTEP — virtual memory / paging.",
    "code": "Demonstrate copy-on-write: fork a process that allocated a large array and watch RSS stay shared until a write."},
   {"t": "The page cache, dirty pages & writeback",
    "concept": "Free RAM isn't wasted: the kernel caches file pages (why the second read is instant); dirty pages + writeback.",
    "watch": "Search 'Linux page cache explained'; Brendan Gregg.",
    "code": "Show the page cache: time a cold vs warm read of a big file; drop_caches and repeat."},
   {"t": "Syscalls & the user/kernel boundary (strace)",
    "concept": "A syscall is a formal request across a security boundary (a mode switch); strace shows every one a process makes.",
    "watch": "Julia Evans — strace zine; search 'Linux syscalls explained'.",
    "code": "strace a real program (e.g. one of your bots) and explain the syscall pattern you see."},
   {"t": "Signals, zombies & the process lifecycle",
    "concept": "fork/exec/wait, signals (how processes are interrupted/terminated), and zombies/orphans.",
    "watch": "OSTEP — process API; Julia Evans — signals.",
    "code": "Write a program that handles signals gracefully, and create (then reap) a zombie to understand it."},
  ], "build": {"title": "Process & memory instrumentation toolkit",
    "A": "Build a tool that, given a PID, reports its memory map (/proc/pid/maps), open fds, and live RSS/threads.",
    "B": "Add a strace-lite mode (ptrace or parse strace) that summarizes the syscalls a process makes.",
    "C": "Run it against your own agent fleet and write up what you learned — a standout systems portfolio project."},
  "cons": {"title": "What a process really is",
    "text": "The address space + /proc, virtual memory/paging, the page cache, syscalls/strace and the process lifecycle are how programs actually run. Re-read one process's /proc.",
    "mastery": "Trace what happens from fork() to a running program, and why copy-on-write makes forking a 4GB process instant."}},

 {"title": "Linux Internals II — I/O, FS & Scheduling", "days": [
   {"t": "Filesystems, the VFS & inodes",
    "concept": "How files are stored: inodes vs names (hard/soft links), the VFS abstraction over ext4/xfs, and directories as data.",
    "watch": "OSTEP — file systems; Julia Evans — inodes.",
    "code": "Explore inodes: create hard/soft links, delete the original, and explain what survives and why."},
   {"t": "File I/O: buffered vs direct, fsync, mmap",
    "concept": "How reads/writes actually hit disk: buffering, fsync for durability, O_DIRECT, and mmap for memory-mapped files.",
    "watch": "Search 'Linux file IO buffered direct fsync'; OSTEP.",
    "code": "Compare throughput of buffered vs fsync-per-write vs mmap for writing a large file."},
   {"t": "The CPU scheduler (CFS), context switches",
    "concept": "How Linux shares CPUs: the Completely Fair Scheduler, nice/priorities, and the cost of context switches.",
    "watch": "OSTEP — scheduling; Brendan Gregg — scheduler.",
    "code": "Observe scheduling: run CPU-bound tasks, change nice values, and measure context switches with vmstat/pidstat."},
   {"t": "epoll & non-blocking I/O",
    "concept": "How servers handle thousands of connections: blocking vs non-blocking I/O and epoll (the engine under async runtimes).",
    "watch": "Search 'epoll explained' and 'C10k problem'.",
    "code": "Write a tiny epoll-based echo server that handles many concurrent connections in one thread."},
   {"t": "Capabilities & security primitives",
    "concept": "Fine-grained privileges (capabilities) beyond root, plus seccomp and namespaces as security building blocks (a bridge to containers).",
    "watch": "Search 'Linux capabilities explained' and 'seccomp'.",
    "code": "Drop capabilities from a process and grant just one (e.g. bind a low port) instead of full root."},
  ], "build": {"title": "Mini shell / high-throughput I/O tool",
    "A": "Build a small Unix shell (C/Python): fork/exec, pipes, redirection and job control — using the syscalls directly.",
    "B": "Or build a high-throughput I/O tool (an epoll server or a parallel file processor) and benchmark it.",
    "C": "Write up the syscalls involved and the performance numbers — a deep systems portfolio piece."},
  "cons": {"title": "How the kernel schedules & serves I/O",
    "text": "Filesystems/inodes, file I/O, the scheduler, epoll and capabilities are the kernel services every program relies on. Re-read your I/O benchmark.",
    "mastery": "Explain the difference between a hard and soft link at the inode level, and how epoll lets one thread serve thousands of connections."}},

 {"title": "Systems Networking", "days": [
   {"t": "The network stack & sockets (ss, ip)",
    "concept": "The layered stack (L2-L7), how a socket connects an app to the network, and inspecting it with ss/ip.",
    "watch": "Julia Evans — networking zines; search 'Linux network stack explained'.",
    "code": "Map a live connection through the stack; use ss to see every socket and its state."},
   {"t": "TCP deep: handshake, congestion control",
    "concept": "The 3-way handshake, flow + congestion control, TIME_WAIT, and why TCP behaves the way it does.",
    "watch": "Search 'TCP handshake congestion control explained'; Julia Evans.",
    "code": "Watch a TCP handshake + teardown in tcpdump/Wireshark and identify each packet."},
   {"t": "DNS, TLS, HTTP/1.1 -> HTTP/3",
    "concept": "How a URL becomes a response: DNS resolution, the TLS handshake, and HTTP/1.1 -> 2 -> 3 (QUIC).",
    "watch": "Julia Evans — DNS/TLS zines; search 'HTTP/2 HTTP/3 explained'.",
    "code": "Trace a full request: dig the DNS, inspect the TLS handshake (openssl s_client), compare HTTP/1.1 vs 2."},
   {"t": "tcpdump / Wireshark",
    "concept": "Packet capture and analysis — the ultimate 'what's actually on the wire' debugging tool.",
    "watch": "Search 'tcpdump tutorial' and 'Wireshark crash course'.",
    "code": "Capture and dissect a real app's traffic; find a specific request/response in the capture."},
   {"t": "Network tuning & throughput",
    "concept": "Bandwidth vs latency, buffer sizes, and diagnosing slow networks — it matters for distributed data/ML systems.",
    "watch": "Brendan Gregg — network performance; search 'Linux network tuning'.",
    "code": "Measure throughput/latency (iperf, ping) between hosts and tune a buffer or MTU setting."},
  ], "build": {"title": "TCP proxy / load balancer + packet analysis",
    "A": "Build a TCP proxy (accept -> connect upstream -> pipe both ways) using sockets; handle many connections.",
    "B": "Turn it into a simple load balancer (round-robin across backends) with health checks.",
    "C": "Analyze its traffic in Wireshark and write up the request journey + your L4 LB design — a flagship networking project."},
  "cons": {"title": "A request's full journey",
    "text": "The stack/sockets, TCP internals, DNS/TLS/HTTP, packet analysis and tuning explain every request your systems make. Re-read your Wireshark capture.",
    "mastery": "Walk a request from URL to response through DNS, TLS, TCP and HTTP, and what tcpdump would show at each step."}},

 {"title": "Containers, Deep", "days": [
   {"t": "Namespaces — the isolation primitives",
    "concept": "The Linux namespaces (pid, net, mnt, uts, ipc, user) that give a container its own view of the system — half of what a container is.",
    "watch": "Liz Rice — 'Containers from scratch'; search 'Linux namespaces explained'.",
    "code": "Use unshare to create a process in its own PID + mount namespace and observe the isolation."},
   {"t": "cgroups — resource limits & accounting",
    "concept": "Control groups that limit and account CPU/memory/IO — the other half of a container; cgroups v2.",
    "watch": "Search 'Linux cgroups explained'; Liz Rice.",
    "code": "Put a process in a cgroup with a memory + CPU limit and watch it get throttled/OOM-killed."},
   {"t": "Docker internals: layers, union fs",
    "concept": "How Docker images work: layered union filesystems (overlayfs), the build cache, and image vs container.",
    "watch": "Search 'Docker images layers overlayfs explained'; TechWorld with Nana — Docker.",
    "code": "Inspect an image's layers, write an efficient multi-stage Dockerfile, and minimize image size."},
   {"t": "Container networking & volumes",
    "concept": "How containers get networking (bridge, veth, port mapping) and persistent storage (volumes, bind mounts).",
    "watch": "Search 'Docker networking explained' and 'Docker volumes'.",
    "code": "Connect two containers over a user-defined network and persist data with a volume."},
   {"t": "Image build & supply-chain security",
    "concept": "Building minimal, secure images: distroless/scratch, non-root users, vulnerability scanning and signing.",
    "watch": "Search 'container image security best practices' and 'distroless images'.",
    "code": "Harden a Dockerfile (non-root, minimal base, scan with trivy) and shrink + secure the image."},
  ], "build": {"title": "Container runtime from scratch",
    "A": "Build a minimal container runtime (Go/Python) using namespaces + cgroups + chroot/pivot_root — the 'Docker in 100 lines' project.",
    "B": "Add a limited rootfs image and resource limits; run a real command inside it.",
    "C": "Write up 'what a container actually is' with your code — a flagship, deeply-credible systems portfolio project."},
  "cons": {"title": "How containers really work",
    "text": "Namespaces, cgroups, Docker internals, container networking and image security show a container is just Linux primitives, not magic. Re-read your runtime code.",
    "mastery": "Explain a container as namespaces + cgroups + a rootfs, and what each namespace isolates."}},

 {"title": "Kubernetes, Deep", "days": [
   {"t": "Architecture: control plane, kubelet",
    "concept": "The K8s architecture: API server, etcd, scheduler, controller-manager (control plane) and the kubelet on each node.",
    "watch": "TechWorld with Nana — Kubernetes crash course; search 'Kubernetes architecture explained'.",
    "code": "Stand up a local cluster (kind/minikube) and inspect the control-plane components."},
   {"t": "Pods, services, deployments, config",
    "concept": "The core objects: pods, deployments (rollouts/replicas), services (networking) and ConfigMaps/Secrets.",
    "watch": "TechWorld with Nana — K8s objects; the Kubernetes docs tutorials.",
    "code": "Deploy an app: Deployment + Service + ConfigMap; scale it and do a rolling update."},
   {"t": "Scheduling, resources & autoscaling",
    "concept": "How pods get placed (requests/limits, affinity, taints), and autoscaling (HPA) based on load.",
    "watch": "Search 'Kubernetes scheduling requests limits HPA'.",
    "code": "Set resource requests/limits, add a HorizontalPodAutoscaler, and load-test the scaling."},
   {"t": "Operators & CRDs",
    "concept": "Extending K8s with CRDs and operators that encode operational knowledge (the pattern behind data/ML platforms on K8s).",
    "watch": "Search 'Kubernetes operators CRD explained'.",
    "code": "Install an operator (e.g. Prometheus/Postgres) via a CRD and use its custom resources."},
   {"t": "GPU scheduling for inference",
    "concept": "Running GPU workloads on K8s: device plugins, requesting GPUs, and scheduling inference/training — ties back to your AI phase.",
    "watch": "Search 'Kubernetes GPU scheduling device plugin'.",
    "code": "Schedule a pod that requests a GPU (or simulate with resource classes) and serve a model on it."},
  ], "build": {"title": "Deploy & operate an app on K8s (Helm)",
    "A": "Package a real app (your RAG/agent or ML API) as a Helm chart: Deployment, Service, ConfigMap/Secret, HPA.",
    "B": "Deploy to a cluster with health checks, resource limits, autoscaling and a rolling-update strategy.",
    "C": "Add an ingress + TLS and document the operations runbook — a portfolio 'I can run K8s' piece."},
  "cons": {"title": "Orchestration at scale",
    "text": "The control plane, core objects, scheduling/autoscaling, operators/CRDs and GPU scheduling are how you run containers in production at scale. Re-read your Helm chart.",
    "mastery": "Trace what happens from 'kubectl apply' to a running pod, and how a Service + Deployment give you self-healing scale."}},

 {"title": "IaC & GitOps", "days": [
   {"t": "Terraform, deep",
    "concept": "Declarative infrastructure with Terraform: providers, resources, state, plan/apply, and modules for reuse.",
    "watch": "TechWorld with Nana — Terraform; HashiCorp Learn.",
    "code": "Provision real cloud infra (a VM + network + a managed DB) with Terraform using variables + a module."},
   {"t": "Ansible & configuration management",
    "concept": "Configuring servers idempotently with Ansible (playbooks, roles, inventory) — the config half of IaC.",
    "watch": "Search 'Ansible tutorial'; TechWorld with Nana.",
    "code": "Write an Ansible playbook that provisions + hardens a server (reuse Week 52) idempotently."},
   {"t": "GitOps with ArgoCD / Flux",
    "concept": "GitOps: git as the single source of truth, with ArgoCD/Flux continuously reconciling the cluster to match.",
    "watch": "Search 'GitOps explained ArgoCD'; TechWorld with Nana.",
    "code": "Set up ArgoCD to auto-deploy your Helm app from git; change git and watch it reconcile."},
   {"t": "Helm & templating",
    "concept": "Helm for templated, versioned K8s deployments: charts, values, releases and rollbacks.",
    "watch": "Search 'Helm tutorial'.",
    "code": "Parameterize your chart with values for dev/prod and practice a release + rollback."},
   {"t": "Secrets management (Vault, SOPS)",
    "concept": "Handling secrets safely in IaC/GitOps: Vault, sealed-secrets/SOPS, and never committing plaintext.",
    "watch": "Search 'secrets management Kubernetes Vault SOPS'.",
    "code": "Encrypt secrets with SOPS (or sealed-secrets) so they can live in git safely, and wire them into a deploy."},
  ], "build": {"title": "Full IaC + GitOps for your platform",
    "A": "Define your whole stack as code: Terraform for cloud infra (cluster, DB, networking), versioned + modular.",
    "B": "Deploy apps via GitOps (ArgoCD/Flux) from git, with Helm charts and encrypted secrets.",
    "C": "Make it fully reproducible ('git clone -> apply -> running platform') and document it — a flagship infra portfolio piece."},
  "cons": {"title": "Infrastructure as code",
    "text": "Terraform, Ansible, GitOps, Helm and secrets management turn infrastructure into versioned, reviewable, reproducible code. Re-run your apply from scratch.",
    "mastery": "What does GitOps give you over 'kubectl apply by hand', and how do you keep secrets in git without leaking them?"}},

 {"title": "Observability & SRE", "days": [
   {"t": "Metrics, logs & traces",
    "concept": "The three pillars of observability — metrics (what), logs (why), traces (where) — and when to use each.",
    "watch": "Google SRE book (monitoring); search 'observability three pillars'.",
    "code": "Instrument an app to emit metrics, structured logs and traces for one request path."},
   {"t": "Prometheus & Grafana",
    "concept": "Metrics collection with Prometheus (scraping, PromQL) and dashboards/alerts with Grafana.",
    "watch": "TechWorld with Nana — Prometheus/Grafana; search 'Prometheus tutorial'.",
    "code": "Scrape your app's metrics with Prometheus and build a Grafana dashboard for it."},
   {"t": "SLOs, SLIs & error budgets",
    "concept": "Defining reliability: SLIs (measured), SLOs (targets) and error budgets that balance reliability vs velocity — the heart of SRE.",
    "watch": "Google SRE book — SLOs; search 'SLI SLO error budget explained'.",
    "code": "Define SLIs + an SLO for your service and compute its error budget from real metrics."},
   {"t": "Alerting & on-call",
    "concept": "Actionable, symptom-based alerting (not noise), on-call practices, and alert fatigue.",
    "watch": "Google SRE book — alerting/on-call.",
    "code": "Write symptom-based alerts (latency/error-rate SLO burn) with Alertmanager, not CPU-noise alerts."},
   {"t": "Incident response & postmortems",
    "concept": "Handling incidents (roles, comms, mitigation) and blameless postmortems that turn failures into improvements.",
    "watch": "Google SRE book — incident management + postmortems.",
    "code": "Run a game-day: break your service, respond, and write a blameless postmortem."},
  ], "build": {"title": "Observability & alerting stack for the platform",
    "A": "Instrument your platform (DE/ML/AI services) with metrics, logs and traces; deploy Prometheus + Grafana.",
    "B": "Define SLIs/SLOs + error budgets and symptom-based alerting for the key user journeys.",
    "C": "Run a game-day incident and write the postmortem — a portfolio SRE piece that shows operational maturity."},
  "cons": {"title": "Running systems reliably",
    "text": "The three pillars, Prometheus/Grafana, SLOs/error budgets, alerting and incident response are how you run systems like an SRE. Re-read your postmortem.",
    "mastery": "What's the difference between an SLI, SLO and error budget, and why should alerts be symptom-based?"}},

 {"title": "Performance & Security", "days": [
   {"t": "perf & eBPF tooling",
    "concept": "Deep performance analysis: perf, flame graphs, and eBPF (bpftrace/BCC) to observe the kernel + apps with almost no overhead.",
    "watch": "Brendan Gregg — perf/eBPF; search 'flame graphs explained'.",
    "code": "Profile a CPU-bound app with perf, generate a flame graph, find the hotspot, and try one bpftrace one-liner."},
   {"t": "System & network tuning",
    "concept": "Finding + fixing bottlenecks across CPU/memory/disk/network using the USE method (Utilization, Saturation, Errors).",
    "watch": "Brendan Gregg — the USE method; 'Linux performance tools'.",
    "code": "Apply the USE method to a loaded system, identify the bottleneck resource, and tune it."},
   {"t": "Linux security: SELinux, seccomp",
    "concept": "Mandatory access control (SELinux/AppArmor), seccomp to restrict syscalls, and defense-in-depth for services/containers.",
    "watch": "Search 'SELinux explained' and 'seccomp containers'.",
    "code": "Apply a seccomp profile to a container to block dangerous syscalls and observe SELinux/AppArmor denials."},
   {"t": "Hardening & secrets",
    "concept": "Server/container hardening (least privilege, minimal surface, updates) and secrets hygiene end to end.",
    "watch": "Search 'Linux server hardening checklist'; CIS benchmarks.",
    "code": "Run a hardening checklist against your server + containers and fix the top findings."},
   {"t": "Cost & resource accounting",
    "concept": "Measuring where compute/cost goes (cgroups accounting, cloud cost), right-sizing, and FinOps for infra.",
    "watch": "Search 'Linux resource accounting cgroups' and cloud cost basics.",
    "code": "Attribute resource usage per service (cgroups/cloud tags) and right-size one over-provisioned component."},
  ], "build": {"title": "Profile, tune & harden the platform",
    "A": "Profile your platform under load (perf/eBPF, USE method) and fix the top performance bottleneck.",
    "B": "Harden it: seccomp/AppArmor on containers, a server hardening checklist, least-privilege everywhere.",
    "C": "Add cost/resource accounting and right-size; document the perf + security + cost wins — a rounded SRE piece."},
  "cons": {"title": "Fast and defensible",
    "text": "perf/eBPF, the USE method, SELinux/seccomp, hardening and cost accounting make systems fast, secure and affordable. Re-read your flame graph.",
    "mastery": "Walk the USE method to find a bottleneck, and name three ways you'd harden a container running untrusted code."}},

 {"title": "Grand Capstone — Productionize", "days": [
   {"t": "Integrate the DE, ML & AI layers",
    "concept": "Bring the three capstones together into one intelligent platform: data flows into features, models serve predictions, an AI layer (RAG/agent) sits on top.",
    "watch": "Review your three capstone architectures and sketch the unified one.",
    "code": "Design + wire the integration: the DE platform feeds the ML models feed the AI layer, end to end."},
   {"t": "Deploy on Kubernetes with IaC",
    "concept": "Deploy the whole platform reproducibly: Terraform for infra, Helm/GitOps for the services.",
    "watch": "Reuse your K8s + IaC + GitOps work.",
    "code": "Deploy every component to K8s via IaC + GitOps; one command from zero to running."},
   {"t": "CI/CD & GitOps",
    "concept": "Automated build/test/deploy for the whole platform, with GitOps reconciliation and safe rollouts.",
    "watch": "Reuse your CI/CD + GitOps work.",
    "code": "Wire CI/CD (test + build + deploy) and GitOps auto-sync for the platform."},
   {"t": "Observability, alerting & SLOs",
    "concept": "Full-stack observability + SLOs for the integrated platform, so you'd know the moment anything degrades.",
    "watch": "Reuse your observability/SRE work.",
    "code": "Add metrics/logs/traces, dashboards, SLOs and alerting across the whole platform."},
   {"t": "Load & failure testing, security",
    "concept": "Prove it holds up: load testing, chaos/failure injection, and a security pass before you call it production.",
    "watch": "Reuse your performance + security work.",
    "code": "Load-test the platform, inject failures (kill pods/deps) to verify resilience, and run a security pass."},
  ], "build": {"title": "Ship the intelligent platform to production",
    "A": "Deploy the fully integrated DE + ML + AI platform on K8s via IaC + GitOps, with CI/CD.",
    "B": "Add full observability + SLOs, then load-test, chaos-test and security-harden it.",
    "C": "This is your capstone: the whole 62-week journey, running in production. Record a demo + architecture walkthrough."},
  "cons": {"title": "Everything, together, in production",
    "text": "Integration, K8s + IaC deploy, CI/CD + GitOps, observability + SLOs and resilience/security are the difference between projects and a platform. Step back and see the whole thing.",
    "mastery": "Give the full architecture tour of your platform — data to features to models to AI to serving — and how it's deployed, observed and kept reliable."}},

 {"title": "Launch & Portfolio", "days": [
   {"t": "System-design writeup",
    "concept": "Write the definitive system-design doc for your platform: the problem, architecture, tradeoffs and lessons.",
    "watch": "Study strong engineering system-design writeups for structure.",
    "code": "Write the full system-design doc (architecture, decisions, tradeoffs, what you'd do next)."},
   {"t": "Architecture diagrams",
    "concept": "Communicate the system visually: clear architecture + data-flow diagrams a stranger can follow.",
    "watch": "Search 'good architecture diagrams C4 model'.",
    "code": "Produce clean architecture + data-flow diagrams for the platform and every capstone."},
   {"t": "Demo & video walkthrough",
    "concept": "Show, don't tell: a crisp demo + recorded walkthrough that proves the work is real.",
    "watch": "Study good project demo videos for pacing.",
    "code": "Record a tight demo/walkthrough of the platform and 2-3 flagship projects."},
   {"t": "Blog posts / talks",
    "concept": "Teach what you built: writeups/talks on the standout projects (GPT from scratch, RAG, the platform) that build your reputation.",
    "watch": "Study strong engineering blog posts for structure.",
    "code": "Write one deep technical blog post on a flagship project ('I built a GPT/container/platform from scratch')."},
   {"t": "Interview & narrative prep",
    "concept": "Turn 62 weeks into a story: the portfolio site, the resume update, and the interview narrative across DE/ML/AI/systems.",
    "watch": "Review your projects and prep the STAR stories.",
    "code": "Update your portfolio + resume and rehearse the 3-minute story for each flagship project."},
  ], "build": {"title": "Portfolio launch — case studies & demos",
    "A": "Assemble everything: the portfolio site, project case studies, diagrams, demos and blog posts.",
    "B": "Polish the top 6-8 flagship projects into strong case studies (problem -> approach -> result).",
    "C": "Launch it publicly (this dashboard, GitHub, LinkedIn) — the capstone of the whole journey and your new professional story."},
  "cons": {"title": "Mastery review & what's next",
    "text": "Writeups, diagrams, demos, blog posts and your narrative turn 62 weeks of work into a career step. Look back at Week 1, then decide what you go deep on next.",
    "mastery": "Deliver your story: who you are now across Data Engineering, ML, AI and systems, and the three projects that prove it."}},
]

# Per-project metadata keyed by week: tech tags, flagship flag, and a resume line
# (demo) for the flagship pieces. Non-flagship demo lines stay empty (blurb suffices).
PROJECT_META = {
 1:  {"tech": ["Python", "pydantic", "pytest", "Typer"]},
 2:  {"tech": ["Python", "cProfile", "NumPy"]},
 3:  {"tech": ["PostgreSQL", "SQL", "indexing"]},
 4:  {"tech": ["Python", "LSM-tree", "systems"], "flag": True, "demo": "Write-optimized LSM-tree storage engine (memtable, SSTables, compaction) built from scratch."},
 5:  {"tech": ["Python", "sharding", "replication"]},
 6:  {"tech": ["Python", "Raft", "consensus"], "flag": True, "demo": "Raft consensus — leader election + replicated log with crash recovery, from scratch."},
 7:  {"tech": ["dbt", "SQL", "Kimball"]},
 8:  {"tech": ["dbt", "warehouse", "SQL"]},
 9:  {"tech": ["Python", "query optimization", "database internals"]},
 10: {"tech": ["Spark", "PySpark", "tuning"]},
 11: {"tech": ["Databricks", "Delta Lake", "PySpark"], "flag": True, "demo": "Medallion lakehouse on Databricks with Delta Lake (bronze/silver/gold, DLT, time travel)."},
 12: {"tech": ["Kafka", "Avro", "Python"]},
 13: {"tech": ["Kafka", "Spark Streaming", "Delta"], "flag": True, "demo": "Exactly-once streaming pipeline: Kafka → Spark Structured Streaming → Delta."},
 14: {"tech": ["Airflow", "Dagster"]},
 15: {"tech": ["distributed systems", "paper reproduction", "Python"]},
 16: {"tech": ["Great Expectations", "lineage"]},
 17: {"tech": ["AWS", "S3", "Glue", "Athena"]},
 18: {"tech": ["Kafka", "dbt", "Airflow", "lakehouse"], "flag": True, "demo": "End-to-end data platform: ingestion → lakehouse → orchestration → data quality."},
 19: {"tech": ["NumPy", "PCA", "linear algebra"]},
 20: {"tech": ["NumPy", "matplotlib", "optimization"]},
 21: {"tech": ["Python", "statistics", "A/B testing"]},
 22: {"tech": ["paper reproduction", "PyTorch", "ablation study"], "flag": True, "demo": "End-to-end reproduction of a landmark ML paper with an ablation study and a written technical report."},
 23: {"tech": ["NumPy", "scikit-learn"]},
 24: {"tech": ["XGBoost", "LightGBM", "Optuna", "SHAP", "from-scratch"]},
 25: {"tech": ["t-SNE", "UMAP", "representation learning", "from-scratch"]},
 26: {"tech": ["Feast", "scikit-learn"]},
 27: {"tech": ["scikit-learn", "SHAP"]},
 28: {"tech": ["Prophet", "ARIMA", "backtesting"]},
 29: {"tech": ["PyTorch", "matrix factorization"], "flag": True, "demo": "Production recommender system (collaborative + matrix factorization + hybrid), served via API."},
 30: {"tech": ["MLflow", "DVC"]},
 31: {"tech": ["FastAPI", "Docker", "Evidently"], "flag": True, "demo": "ML model served as an API with drift monitoring and CI/CD."},
 32: {"tech": ["system design", "PyTorch"]},
 33: {"tech": ["MLflow", "FastAPI", "Docker"], "flag": True, "demo": "End-to-end ML product: data → features → model → served API with monitoring."},
 34: {"tech": ["NumPy", "PyTorch", "autograd"]},
 35: {"tech": ["PyTorch", "ResNet", "ViT", "paper reproduction"], "flag": True, "demo": "From-the-paper reproduction of ResNet/ViT with an ablation study on its core mechanism."},
 36: {"tech": ["PyTorch", "transfer learning", "CNN"]},
 37: {"tech": ["PyTorch", "NLP", "seq2seq"]},
 38: {"tech": ["PyTorch", "Transformer", "nanoGPT"], "flag": True, "demo": "GPT / Transformer implemented from scratch in PyTorch (nanoGPT-style)."},
 39: {"tech": ["PyTorch", "GPT", "tokenizers"], "flag": True, "demo": "Trained a small GPT from scratch on a custom corpus (tokenizer, training loop, sampling)."},
 40: {"tech": ["LLM API", "prompting", "evals"]},
 41: {"tech": ["RAG", "pgvector", "embeddings", "LLM"], "flag": True, "demo": "Production RAG over a real corpus: hybrid retrieval, reranking, citations, eval harness."},
 42: {"tech": ["LoRA", "PEFT", "HuggingFace"], "flag": True, "demo": "Fine-tuned an open LLM with LoRA/QLoRA and served it."},
 43: {"tech": ["LLM agents", "tools", "function-calling"], "flag": True, "demo": "Autonomous tool-using agent (framework-light) that completes a real task."},
 44: {"tech": ["MCP", "agents", "Python"], "flag": True, "demo": "MCP server + agent exposing a data platform as LLM tools."},
 45: {"tech": ["vLLM", "LLMOps", "guardrails"]},
 46: {"tech": ["CLIP", "contrastive learning", "multimodal", "PyTorch"]},
 47: {"tech": ["AI product", "evals"]},
 48: {"tech": ["RAG", "agents", "LLMOps"], "flag": True, "demo": "Production AI system: RAG + agent + evals + guardrails."},
 49: {"tech": ["Linux", "ptrace", "syscalls", "C"]},
 50: {"tech": ["bash", "awk", "sed"]},
 51: {"tech": ["Linux", "process management", "systemd", "C"]},
 52: {"tech": ["Linux", "SSH", "firewall", "hardening"]},
 53: {"tech": ["Linux", "/proc", "strace"]},
 54: {"tech": ["C", "syscalls", "epoll"]},
 55: {"tech": ["sockets", "TCP", "Wireshark"]},
 56: {"tech": ["namespaces", "cgroups", "Go"], "flag": True, "demo": "Container runtime built from scratch (namespaces + cgroups + rootfs)."},
 57: {"tech": ["Kubernetes", "Helm"]},
 58: {"tech": ["Terraform", "ArgoCD", "GitOps"], "flag": True, "demo": "Infrastructure-as-code + GitOps: Terraform + ArgoCD, reproducible from git."},
 59: {"tech": ["Prometheus", "Grafana", "SRE"]},
 60: {"tech": ["perf", "eBPF", "security"]},
 61: {"tech": ["Kubernetes", "Terraform", "CI/CD"], "flag": True, "demo": "Intelligent data platform (DE + ML + AI) shipped to production on Kubernetes."},
 62: {"tech": ["portfolio", "docs", "demos"]},
}

# ---------------------------------------------------------------- assembly ---
def phase_of(w):
    if w <= 18:
        return "Data Engineering"
    if w <= 33:
        return "Data Science & ML"
    if w <= 48:
        return "Deep Learning & AI"
    return "Linux & Systems"


# "Deep dive" weeks — valuable depth, but deferrable on a time-boxed CORE path.
# The other 54 weeks form a focused ~12-month core. (Adjust this set freely.)
DEEP_WEEKS = {16, 25, 28, 46, 53, 54, 59, 60}

units = []
weeks_meta = []
uid = 0
for wi, wk in enumerate(WEEKS, start=1):
    weeks_meta.append({"n": wi, "title": wk["title"], "phase": phase_of(wi), "core": wi not in DEEP_WEEKS})
    for di, d in enumerate(wk["days"]):
        uid += 1
        text = f"{d['concept']}\n\U0001F3A5 Watch: {d['watch']}\n\U0001F4BB Code: {d['code']}"
        units.append({"week": wi, "dow": di, "type": "theory", "title": d["t"],
                      "text": text, "mastery": None, "id": uid})
    uid += 1
    b = wk["build"]
    btext = f"[Block A] {b['A']} [Block B] {b['B']} [Block C] {b['C']}"
    pm = PROJECT_META.get(wi, {})
    units.append({"week": wi, "dow": 5, "type": "build", "title": b["title"],
                  "text": btext, "mastery": None, "id": uid,
                  "tech": pm.get("tech", []), "flag": pm.get("flag", False),
                  "demo": pm.get("demo", "")})
    uid += 1
    c = wk["cons"]
    units.append({"week": wi, "dow": 6, "type": "consolidate", "title": c["title"],
                  "text": c["text"], "mastery": c["mastery"], "id": uid})

out = os.path.join(os.path.dirname(os.path.abspath(__file__)), "plan_v2.json")
with open(out, "w", encoding="utf-8") as f:
    json.dump({"units": units, "weeks": weeks_meta}, f, indent=1, ensure_ascii=False)
print(f"weeks={len(WEEKS)} units={len(units)} -> {out}")
